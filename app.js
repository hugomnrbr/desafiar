(()=>{
  const cfg=window.SUPABASE_CONFIG||{};
  const ready=!!(window.supabase&&cfg.url&&!String(cfg.url).includes('COLE_AQUI')&&cfg.key&&!String(cfg.key).includes('COLE_AQUI'));
  const sb=ready?window.supabase.createClient(cfg.url,cfg.key):null;
  const S={resultSoundPlayed:false,clockToken:0,route:'home',category:'Geral',mode:'1v1',questions:[],questionIndex:0,matchId:null,match:null,myScore:0,seconds:10,timeLimit:10,timer:null,answered:false,waiting:false,user:null,profile:null,isAdmin:false,realtime:null,poll:null,fiftyUsed:false,plusUsed:false,profileTargetId:null,profileDetails:null,adminQuestions:[],adminSearch:'',adminCategory:'Todos',rankRows:null,rankCategory:'__global__',topicCategory:'',topicRows:null,topicStats:null,recentRows:null,friendRows:null,categories:[],categorySearch:'',achievementsRows:null,notifications:[],notificationUnread:0,notificationActors:{},notificationsLoaded:false,notificationRealtime:null,enterMultiplayer:false,adminCategories:[],adminCategorySubmissions:[],adminAchievements:[],adminAchievementSearch:'',chatTargetId:null,chatTarget:null,chatRows:[],challengeRows:[],challengeTargetId:null,challengeModal:false,challengeSearch:'',searching:false,searchStartedAt:0,searchTimer:null,botOffer:false,challengeRealtime:null,accessToken:null,forfeitSent:false,botMode:false,bot:null,asyncMode:false,asyncChallengeId:null,asyncProgress:0,asyncStartedAt:0,asyncAnswered:false,matchPlayers:{},matchCountdown:0,countdownTimer:null,matchSyncTimer:null,presenceTimer:null,resultPresenceTimer:null,rematchMessage:'',transitionUntil:0,transitionQuestion:-1,queueAvatars:[],queueAvatarsTimer:null,emojiOpen:false,lastAdded:0,timeoutInFlight:false,lastClockSecond:null,clockOffsetMs:0,clockSyncToken:0,soundOn:localStorage.getItem('quizup_sound')!=='off',audio:null};
  const CATS=[['Geral','🌐','c1'],['Ciência','⚗','c2'],['Entretenimento','🎬','c3'],['Esportes','⚽','c4'],['História','🏛','c5'],['Geografia','📍','c6']];
  const catFallback=()=>CATS.map((c,i)=>({id:`default-${i}`,name:c[0],icon:c[1],parent_id:null,approved:true}));
  const catClass=i=>['c1','c2','c3','c4','c5','c6'][i%6];
  const $=s=>document.querySelector(s), $$=s=>document.querySelectorAll(s), esc=s=>String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));
  function audio(){if(!S.soundOn)return null;try{if(!S.audio)S.audio=new (window.AudioContext||window.webkitAudioContext)();if(S.audio.state==='suspended')S.audio.resume();return S.audio}catch(e){return null}}
  function tone(freq,dur=.12,type='sine',gain=.045,delay=0){const a=audio();if(!a)return;const o=a.createOscillator(),g=a.createGain();o.type=type;o.frequency.setValueAtTime(freq,a.currentTime+delay);g.gain.setValueAtTime(.0001,a.currentTime+delay);g.gain.exponentialRampToValueAtTime(gain,a.currentTime+delay+.01);g.gain.exponentialRampToValueAtTime(.0001,a.currentTime+delay+dur);o.connect(g).connect(a.destination);o.start(a.currentTime+delay);o.stop(a.currentTime+delay+dur+.02)}
  function sound(name){if(!S.soundOn)return;audio();if(name==='click')tone(520,.06,'sine',.025);else if(name==='count')tone(620,.16,'triangle',.06);else if(name==='go'){tone(740,.14,'triangle',.07);tone(980,.2,'triangle',.06,.1)}else if(name==='correct'){tone(660,.1,'sine',.045);tone(880,.18,'sine',.05,.08)}else if(name==='wrong'){tone(180,.16,'sawtooth',.035);tone(120,.2,'sawtooth',.025,.08)}else if(name==='timeout'){tone(260,.14,'square',.035);tone(190,.28,'square',.025,.12)}else if(name==='match'){tone(440,.12,'triangle',.04);tone(660,.12,'triangle',.05,.1);tone(990,.25,'triangle',.065,.2)}else if(name==='win'){tone(523,.12,'triangle',.05);tone(659,.12,'triangle',.055,.12);tone(784,.3,'triangle',.07,.24)}else if(name==='lose'){tone(330,.16,'sine',.04);tone(247,.3,'sine',.035,.14)}}
  function av(v='J',c='avatar',url=''){return url?`<img class="${c} avatar-img" src="${esc(url)}" alt="Avatar">`:`<div class="${c}">${esc(v).slice(0,1).toUpperCase()}</div>`}
  function go(r){S.clockToken++;clearInterval(S.timer);clearTimeout(S.poll);clearInterval(S.searchTimer);clearInterval(S.countdownTimer);clearInterval(S.matchSyncTimer);S.matchSyncTimer=null;clearInterval(S.presenceTimer);S.presenceTimer=null;clearInterval(S.resultPresenceTimer);S.resultPresenceTimer=null;clearInterval(S.queueAvatarsTimer);S.queueAvatarsTimer=null;if(S.realtime&&r!=='chat'){sb?.removeChannel(S.realtime);S.realtime=null}S.route=r;render()}
  function sendForfeit(){if(!sb||!S.matchId||S.botMode||S.forfeitSent||!S.user)return; if(S.asyncMode)return; if(S.route!=='game'&&S.route!=='match')return; S.forfeitSent=true; const url=String(cfg.url).replace(/\/$/,'')+'/rest/v1/rpc/forfeit_match'; const body=JSON.stringify({p_match_id:S.matchId}); const headers={'Content-Type':'application/json','apikey':cfg.key,'Authorization':`Bearer ${S.accessToken||''}`,'Prefer':'return=minimal'}; try{fetch(url,{method:'POST',headers,body,keepalive:true,credentials:'omit'}).catch(()=>{})}catch(e){}}
  window.addEventListener('pagehide',sendForfeit);
  window.addEventListener('beforeunload',sendForfeit);
  function goProfile(id=S.user?.id){S.profileTargetId=id;S.profileDetails=null;go('profile')}
  function top(){
    if(['game','async-game','match','result'].includes(S.route))return '';
    const titles={home:'Início',categories:'Categorias',topic:S.topicCategory||'Tópico',ranking:'Ranking',profile:S.profileTargetId===S.user?.id?'Meu Perfil':'Perfil',friends:'Amigos',chat:S.chatTarget?.username||'Chat',notifications:'Notificações',achievements:'Conquistas',contribute:'Criar conteúdo',admin:'Painel'};
    const title=titles[S.route]||'QuizUp';
    return `<header class="classic-top"><button class="top-gear" id="classicHome" title="Início">⚙</button><b>${esc(title)}</b><div class="top-right"><button class="top-icon" id="topSearch" title="Pesquisar">⌕</button>${S.user?`<button class="top-icon notify-btn" id="notificationsBtn" title="Notificações">●${S.notificationUnread>0?`<span class="notify-badge">${S.notificationUnread>99?'99+':S.notificationUnread}</span>`:''}</button><button class="top-avatar" id="topProfile">${av(S.profile?.display_name||'J','avatar mini',S.profile?.avatar_url||'')}</button>${S.isAdmin?'<button class="top-icon" id="adminBtn">⚙</button>':''}`:''}</div></header>`;
  }

  function bottom(){
    if(['game','async-game','match','result','login','signup','admin','contribute','chat','topic'].includes(S.route))return '';
    return `<nav class="classic-bottom"><button data-r="home" class="classic-nav"><span>⌂</span><small>Início</small></button><button data-r="categories" class="classic-nav"><span>▦</span><small>Categorias</small></button><button id="quickPlay" class="classic-play"><span>ϟ</span></button><button data-r="friends" class="classic-nav"><span>♧</span><small>Amigos</small></button><button data-r="notifications" class="classic-nav"><span>♟</span><small>Notícias</small></button></nav>`;
  }

  function render(){document.getElementById('app').innerHTML=`<div class="app">${top()}${view()}</div>${bottom()}`;bind();nav()}
  function nav(){$$('.classic-bottom button').forEach(b=>b.classList.toggle('active',b.dataset.r===S.route))}
  function view(){if(!S.user&&!['login','signup'].includes(S.route))return login();if(S.route==='login')return login();if(S.route==='signup')return signup();if(S.route==='home')return home();if(S.route==='categories')return categories();if(S.route==='topic')return topic();if(S.route==='contribute')return contribute();if(S.route==='match')return match();if(S.route==='game')return game();if(S.route==='async-game')return asyncGame();if(S.route==='result')return result();if(S.route==='ranking')return rank();if(S.route==='profile')return profile();if(S.route==='friends')return friends();if(S.route==='chat')return chat();if(S.route==='notifications')return notificationsView();if(S.route==='admin')return admin();return achievements()}

  function login(){return `<div class="card pad auth-card"><div class="bolt auth-logo">ϟ</div><h1>Entrar no QuizUp</h1><p class="subtitle">Use seu nome de usuário ou e-mail para entrar.</p><form id="loginForm" class="form" style="text-align:left;margin-top:20px"><label>Nome de usuário ou e-mail</label><input id="identifier" required autocomplete="username" placeholder="usuario ou seu@email.com"><label>Senha</label><input id="password" type="password" required autocomplete="current-password" placeholder="••••••••"><button class="primary">ENTRAR</button></form><button class="secondary" id="signupGo">CRIAR CONTA</button>${!ready?'<div class="notice" style="margin-top:12px">Supabase não configurado. Configure <b>config.js</b> para entrar no multiplayer.</div>':''}</div>`}
  function signup(){return `<div class="card pad"><div class="title">Criar conta</div><div class="subtitle">Seu nome de usuário será único e aparecerá no ranking e para seus amigos.</div><form id="signupForm" class="form" style="margin-top:18px"><label>Nome de usuário</label><input id="username" required minlength="3" maxlength="20" pattern="[A-Za-z0-9_]{3,20}" autocomplete="username" placeholder="Ex.: Jogador123"><small class="muted">Use de 3 a 20 caracteres: letras, números e _</small><label>E-mail</label><input id="email" type="email" required autocomplete="email" placeholder="seu@email.com"><label>Senha</label><input id="password" type="password" required minlength="6" autocomplete="new-password" placeholder="mínimo 6 caracteres"><button class="primary">CRIAR CONTA</button></form><button class="secondary" id="loginGo">JÁ TENHO CONTA</button></div>`}
  function home(){
    const p=S.profile||{}; const rec=S.recentRows||[];
    if(!S.recentRows) setTimeout(loadRecentResults,0);
    return `<section class="classic-home">
      <div class="home-hero" style="--cover:${JSON.stringify(p.avatar_url||'')}" >${av(p.display_name||p.username||'J','avatar home-avatar',p.avatar_url||'')}<div class="home-name"><h1>${esc(p.username||p.display_name||'Jogador')}</h1><div>${esc(p.display_name&&p.display_name!==p.username?p.display_name:'Jogador')} • Nível ${p.level||1}</div></div><div class="home-stats"><span><b>${p.wins||0}</b><small>JOGOS</small></span><span><b>${p.wins||0}</b><small>VITÓRIAS</small></span><span><b>${p.losses||0}</b><small>DERROTAS</small></span></div></div>
      <button class="classic-myquiz" id="play"><span>✦</span><div><b>MEU QUIZUP</b><small>Jogar agora e continuar evoluindo</small></div><strong>›</strong></button>
      <div class="classic-section-head"><b>Meus tópicos</b><button data-go="categories">VER TODAS</button></div>
      <div class="followed-grid">${(S.categories||catFallback()).slice(0,8).map((c,i)=>`<button class="followed-topic ${catClass(i)}" data-topic="${esc(c.name)}"><span>${esc(c.icon||'✦')}</span><b>${esc(c.name)}</b><small>Nível ${topicLevel(c.name)}</small></button>`).join('')}</div>
      <div class="classic-section-head"><b>Atividade recente</b><button data-go="ranking">RANKING</button></div>
      <div class="activity-feed">${rec.length?rec.slice(0,4).map(r=>`<div class="activity-item"><div class="activity-dot">${r.won?'✓':'×'}</div><div><b>${r.won?'Você venceu':'Você jogou'} em ${esc(r.category)}</b><small>${r.score} pontos • ${new Date(r.created_at).toLocaleDateString('pt-BR')}</small></div></div>`).join(''):'<div class="classic-empty">Jogue sua primeira partida para começar seu histórico.</div>'}</div>
      <div class="classic-shortcuts"><button data-go="friends">♧ Amigos</button><button data-go="achievements">★ Conquistas</button><button data-go="profile">♙ Perfil</button></div>
    </section>`;
  }

  function categories(){
    const cats=S.categories.length?S.categories:catFallback(); const term=S.categorySearch.trim().toLowerCase();
    const filtered=cats.filter(c=>!term||c.name.toLowerCase().includes(term)||(c.description||'').toLowerCase().includes(term));
    const mains=filtered.filter(c=>!c.parent_id), subs=filtered.filter(c=>c.parent_id);
    const renderCat=(c,i)=>`<button class="cat classic-cat ${catClass(i)}" data-cat="${esc(c.name)}"><span>${esc(c.icon||'✦')}</span><b>${esc(c.name)}</b><small>Nível ${topicLevel(c.name)}</small></button>`;
    return `<section class="classic-page"><div class="classic-banner"><b>Escolha seu próximo desafio</b><span>Compita em tópicos e suba de nível em cada um deles.</span></div><div class="searchbar classic-search"><input id="categorySearch" value="${esc(S.categorySearch)}" placeholder="🔎 Buscar tópicos"><button id="clearCategorySearch" type="button">×</button></div><div class="classic-section-head"><b>Novos e atualizados</b><button id="contributeGo">CRIAR</button></div><div class="topic-grid">${mains.slice(0,12).map(renderCat).join('')}</div>${subs.length?`<div class="classic-section-head"><b>Subtópicos</b></div><div class="topic-grid">${subs.map(renderCat).join('')}</div>`:''}<div class="classic-section-head"><b>Todos os tópicos</b></div><div class="topic-list">${filtered.slice(0,60).map((c,i)=>`<button class="topic-list-row" data-cat="${esc(c.name)}"><span class="topic-icon ${catClass(i)}">${esc(c.icon||'✦')}</span><span><b>${esc(c.name)}</b><small>${c.parent_id?'Subtópico':'Tópico'} • Nível ${topicLevel(c.name)}</small></span><strong>›</strong></button>`).join('')||'<div class="classic-empty">Nenhum tópico encontrado.</div>'}</div></section>`;
  }
  function topicLevel(category){
    const rows=(S.recentRows||[]).filter(r=>r.category===category); const xp=rows.reduce((n,r)=>n+Number(r.score||0),0); return Math.max(1,1+Math.floor(xp/1000));
  }
  async function loadRecentResults(){if(!sb||!S.user)return;const {data}=await sb.from('game_results').select('category,score,won,created_at').eq('user_id',S.user.id).order('created_at',{ascending:false}).limit(40);S.recentRows=data||[];render()}
  async function loadTopicData(){if(!sb||!S.user||!S.topicCategory)return;const [{data:mine},{data:ranking}]=await Promise.all([sb.from('game_results').select('score,won,created_at').eq('user_id',S.user.id).eq('category',S.topicCategory),sb.rpc('get_topic_ranking',{p_category:S.topicCategory,p_limit:20})]);const rows=mine||[];const xp=rows.reduce((n,r)=>n+Number(r.score||0),0);S.topicStats={xp,games:rows.length,wins:rows.filter(r=>r.won).length,losses:rows.filter(r=>!r.won).length,level:Math.max(1,1+Math.floor(xp/1000))};S.topicRows=ranking||[];render()}
  function topic(){if(!S.topicStats){setTimeout(loadTopicData,0);return `<section class="topic-detail loading-topic"><div class="rings small-rings"><div class="big">ϟ</div></div><div class="muted">Carregando tópico...</div></section>`}const t=S.topicStats;const pct=Math.min(100,(t.xp%1000)/10);return `<section class="topic-detail"><div class="topic-cover"><span class="topic-cover-icon">✦</span><h1>${esc(S.topicCategory)}</h1><p>Nível ${t.level}</p></div><button class="classic-big-play" id="topicPlay">JOGAR AGORA <span>⚡</span></button><div class="topic-progress"><div class="progress-head"><b>Nível ${t.level}</b><span>${t.xp%1000} / 1000 XP</span></div><div class="topic-progress-bar"><i style="width:${pct}%"></i></div></div><div class="classic-stat-grid"><div><b>${t.games}</b><small>JOGOS</small></div><div><b>${t.wins}</b><small>VITÓRIAS</small></div><div><b>${t.losses}</b><small>DERROTAS</small></div><div><b>${t.games?Math.round(t.wins/t.games*100):0}%</b><small>VITÓRIAS</small></div></div><div class="classic-section-head"><b>Ranking de ${esc(S.topicCategory)}</b><button id="topicRanking">VER RANKING</button></div><div class="list topic-ranking">${(S.topicRows||[]).slice(0,10).map((r,i)=>`<button class="row player-row" data-profile="${r.id}"><b>#${i+1}</b>${av(r.username||'J','avatar mini',r.avatar_url||'')}<div><b>${esc(r.username||r.display_name||'Jogador')}</b><div class="muted">Nível ${r.level||1} • ${r.wins||0} vitórias</div></div><strong>${r.topic_xp||0}</strong></button>`).join('')||'<div class="classic-empty">Ainda não há jogadores classificados neste tópico.</div>'}</div><button class="secondary" id="backTopics">← VOLTAR AOS TÓPICOS</button></section>`}


  function contribute(){
    const cats=S.categories.length?S.categories:catFallback();
    const n=10;
    const qcards=Array.from({length:n},(_,i)=>`<div class="category-question-card"><div class="cq-head"><b>Pergunta ${i+1} de ${n}</b><span>4 respostas • 1 correta</span></div><textarea class="cq-text" id="cqText${i}" required maxlength="300" placeholder="Digite a pergunta ${i+1}"></textarea><div class="cq-options"><input id="cq0_${i}" required maxlength="160" placeholder="A) Resposta"><input id="cq1_${i}" required maxlength="160" placeholder="B) Resposta"><input id="cq2_${i}" required maxlength="160" placeholder="C) Resposta"><input id="cq3_${i}" required maxlength="160" placeholder="D) Resposta"></div><select id="cqCorrect${i}" required><option value="0">A é a correta</option><option value="1">B é a correta</option><option value="2">C é a correta</option><option value="3">D é a correta</option></select></div>`).join('');
    return `<section class="contribute-page"><div class="title">Criar novo tópico</div><div class="subtitle">Crie uma categoria original e prepare <b>pelo menos 10 perguntas</b>. Todas as perguntas precisam ter 4 alternativas, sendo 1 correta e 3 erradas. Depois de enviar, tudo será analisado pelo administrador antes de ficar disponível para os jogadores.</div><form id="categoryPackageForm" class="form"><div class="card pad"><h3>1. Dados da categoria</h3><input id="packageCatName" required maxlength="60" placeholder="Nome da categoria"><input id="packageCatIcon" maxlength="4" placeholder="Ícone, ex.: ⚽"><textarea id="packageCatDesc" maxlength="180" placeholder="Descrição da categoria"></textarea></div><div class="card pad" style="margin-top:12px"><h3>2. Suas 10 perguntas</h3><p class="muted">As 10 perguntas abaixo são obrigatórias. Você poderá acrescentar mais depois em uma nova versão.</p>${qcards}</div><div class="card pad" style="margin-top:12px"><div class="notice"><b>Antes de enviar:</b> confira as 10 perguntas, as 4 alternativas de cada uma e marque corretamente qual alternativa é a certa. O administrador receberá o pacote completo para aprovação.</div><button class="primary" type="submit" id="submitCategoryPackage">ENVIAR CATEGORIA E 10 PERGUNTAS</button></div></form><button class="secondary" id="backCategories">← VOLTAR ÀS CATEGORIAS</button></section>`;
  }
  function match(){
    if(S.match&&S.matchCountdown>0){const ids=(S.match.player_ids||[]).filter(id=>id!==S.user.id);const opponent=ids[0]?S.matchPlayers[ids[0]]:S.bot;const name=opponent?.username||opponent?.display_name||'Oponente';return `<div class="classic-match-found"><div class="loading-bolt">ϟ</div><div class="classic-match-label">ADVERSÁRIO ENCONTRADO</div><div class="match-vs"><div class="match-player">${av(S.profile?.display_name||'J','avatar huge',S.profile?.avatar_url||'')}<b>${esc(S.profile?.username||'Você')}</b></div><div class="vs-glow">VS</div><div class="match-player">${av(opponent?.username||'R','avatar huge',opponent?.avatar_url||'')}<b>${esc(name)}</b></div></div><div class="countdown-label">Começando em</div><div class="countdown-number">${S.matchCountdown}</div></div>`}
    if(S.match&&S.matchCountdown===0)return `<div class="classic-loading"><div class="loading-bolt">ϟ</div><b>CARREGANDO AS PERGUNTAS</b></div>`;
    const elapsed=Math.floor((Date.now()-(S.searchStartedAt||Date.now()))/1000); if(!S.searching)setTimeout(()=>findMatch(),0);
    if(S.botOffer)return `<div class="classic-loading"><div class="loading-bolt">ϟ</div><h2>Nenhum jogador encontrado</h2><p class="muted">Você pode continuar procurando ou jogar contra o QuizBot.</p><button class="primary" id="playBot">JOGAR CONTRA ROBÔ</button><button class="secondary" id="continueReal">CONTINUAR PROCURANDO</button><button class="secondary" id="cancel">CANCELAR</button></div>`;
    return `<div class="classic-loading"><div class="loading-bolt pulse">ϟ</div><b>PROCURANDO ADVERSÁRIO</b><div class="search-seconds">${elapsed}s</div><div class="muted">Tópico: ${esc(S.category)}</div><div class="queue-avatars">${(S.queueAvatars||[]).slice(0,5).map(p=>av('J','avatar queue-avatar',p.avatar_url||'')).join('')}${(S.queueAvatars||[]).length>5?'<span class="queue-more">•••</span>':''}</div><button class="secondary" id="cancel">CANCELAR</button></div>`;
  }


  async function loadQueueAvatars(){if(!sb||S.route!=='match'||!S.searching)return;const {data}=await sb.rpc('get_queue_avatars',{p_mode:S.mode,p_category:S.category});if(Array.isArray(data)){S.queueAvatars=data;render();}}
  async function findMatch(){
    if(!sb){alert('Supabase não está configurado. Configure o config.js para usar o multiplayer.');return go('login');}
    if(S.searching && S.poll)return;
    S.searching=true;S.botOffer=false;S.searchStartedAt=S.searchStartedAt||Date.now();render();
    clearInterval(S.searchTimer);
    S.searchTimer=setInterval(()=>{if(S.route!=='match'||!S.searching)return;const elapsed=Math.floor((Date.now()-S.searchStartedAt)/1000);if(elapsed>=15){clearInterval(S.searchTimer);clearTimeout(S.poll);S.poll=null;S.botOffer=true;render();}else render()},1000);
    clearInterval(S.queueAvatarsTimer);S.queueAvatarsTimer=setInterval(loadQueueAvatars,1500);loadQueueAvatars();
    const attempt=async()=>{
      if(!S.searching||S.botOffer)return;
      const elapsed=(Date.now()-S.searchStartedAt)/1000;
      if(elapsed>=15){S.botOffer=true;clearInterval(S.searchTimer);S.poll=null;return render()}
      // Cada tentativa envia um heartbeat da fila. Assim, somente jogadores
      // realmente conectados e procurando agora podem formar um match.
      const {data,error}=await sb.rpc('find_or_create_match',{p_mode:S.mode,p_category:S.category});
      if(error){console.error(error);alert(error.message);S.searching=false;return go('categories')}
      if(data){S.searching=false;clearInterval(S.queueAvatarsTimer);S.queueAvatarsTimer=null;await openMatch(data);return}
      S.poll=setTimeout(attempt,1200);
    };
    attempt();
  }
  async function loadMatchPlayers(data){
    const ids=(data.player_ids||[]).filter(id=>id!==S.user.id);
    if(!ids.length)return;
    const {data:rows}=await sb.from('profiles').select('id,username,display_name,avatar_url').in('id',ids);
    const map={};(rows||[]).forEach(x=>map[x.id]=x);S.matchPlayers=map;
  }
  function startMatchCountdown(){
    clearInterval(S.countdownTimer);clearInterval(S.matchSyncTimer);S.matchSyncTimer=null;
    S.matchCountdown=3;sound('match');render();
    let last=3;
    S.countdownTimer=setInterval(async()=>{
      S.matchCountdown--;
      if(S.matchCountdown>0){sound('count');render();return;}
      clearInterval(S.countdownTimer);S.countdownTimer=null;
      if(S.botMode){
        const started=Date.now();
        S.roundStartedAt=started;S.match.state={...(S.match.state||{}),question_started_at:started/1000};
        S.route='game';sound('go');render();startClock();scheduleBotTurn();return;
      }
      if(sb&&S.matchId){
        const {data,error}=await sb.rpc('start_match_round',{p_match_id:S.matchId});
        if(error){console.error(error);return go('categories')}
        if(data){S.match=data;S.questionIndex=Number(data.current_question||0);S.roundStartedAt=Number(data.state?.question_started_at||0)*1000;S.seconds=10;S.answered=false;S.waiting=false;}
      }
      S.route='game';sound('go');render();startClock();
    },1000);
  }
  async function matchPresenceBeat(){if(!sb||!S.matchId||S.botMode||!S.user||!['match','game'].includes(S.route))return;const {data,error}=await sb.rpc('check_match_presence',{p_match_id:S.matchId});if(error){console.warn('presence',error.message);return}if(data?.finished){clearInterval(S.presenceTimer);S.presenceTimer=null;const {data:m}=await sb.from('matches').select('*').eq('id',S.matchId).single();if(m)await syncMatch(m)}}
  function startMatchPresence(){clearInterval(S.presenceTimer);if(!sb||!S.matchId||S.botMode)return;matchPresenceBeat();S.presenceTimer=setInterval(matchPresenceBeat,2000)}
  async function openMatch(id){
    clearTimeout(S.poll);clearInterval(S.searchTimer);S.poll=null;S.searching=false;S.matchId=id;
    const {data,error}=await sb.from('matches').select('*').eq('id',id).single();
    if(error||!data){alert(error?.message||'Não foi possível abrir a partida.');return go('categories')}
    S.match=data;S.asyncMode=false;S.asyncChallengeId=null;S.resultSoundPlayed=false;S.rematchMessage='';S.transitionUntil=0;S.transitionQuestion=-1;S.botMode=false;S.bot=null;S.mode=data.mode;S.category=data.category;S.questionIndex=data.current_question||0;S.answered=false;S.waiting=false;S.timeoutInFlight=false;S.lastClockSecond=null;S.clockOffsetMs=0;S.fiftyUsed=false;S.plusUsed=false;await loadMatchPlayers(data);await loadMatchQuestions();subscribeMatch();S.route='match';startMatchPresence();startMatchCountdown();
  }
  async function loadMatchQuestions(){
    if(!sb||!S.match?.question_ids?.length){alert('A partida não possui perguntas do Supabase. Execute o seed-300-questions.sql.');return go('categories');}
    const {data,error}=await sb.from('questions').select('id,question_text,options,correct_index,image_url').in('id',S.match.question_ids);
    if(error||!data||data.length<7){alert('Esta partida não possui 7 perguntas válidas. Execute o seed-300-questions.sql no Supabase.');return go('categories')}
    const map=new Map(data.map(q=>[q.id,q]));S.questions=S.match.question_ids.map(id=>map.get(id)).filter(Boolean);
  }
  function subscribeMatch(){
    if(!sb||!S.matchId)return;
    if(S.realtime)sb.removeChannel(S.realtime);
    S.realtime=sb.channel(`match-${S.matchId}`).on('postgres_changes',{event:'UPDATE',schema:'public',table:'matches',filter:`id=eq.${S.matchId}`},async payload=>{await syncMatch(payload.new)}).subscribe();
    clearInterval(S.matchSyncTimer);
    // Realtime é o canal principal; este polling curto é um fallback para garantir
    // que nenhum UPDATE seja perdido e os dois jogadores permaneçam na mesma pergunta.
    S.matchSyncTimer=setInterval(async()=>{
      if(S.route!=='game'||!S.matchId||S.botMode)return;
      const {data}=await sb.from('matches').select('*').eq('id',S.matchId).single();
      if(data)await syncMatch(data);
    },900);
  }
  async function syncMatch(m){
    if(!m||S.botMode)return;
    const serverQuestion=Number(m.current_question||0);
    const localQuestion=Number(S.questionIndex||0);
    if(S.match&&serverQuestion<localQuestion)return;
    const oldStarted=Number(S.match?.state?.question_started_at||0);
    const newStarted=Number(m.state?.question_started_at||0);
    S.match=m;S.mode=m.mode;S.category=m.category;
    if(m.status==='finished'){
      clearInterval(S.timer);clearInterval(S.matchSyncTimer);S.matchSyncTimer=null;
      await finishOnline();return;
    }
    if(serverQuestion!==localQuestion){
      S.questionIndex=serverQuestion;S.answered=false;S.waiting=false;S.timeoutInFlight=false;S.lastClockSecond=null;S.clockOffsetMs=0;S.lastAdded=0;S.fiftyUsed=false;S.plusUsed=false;S.timeLimit=10;S.seconds=10;S.roundStartedAt=newStarted*1000;
      if(S.route==='game' && newStarted*1000>Date.now()+100){S.transitionUntil=newStarted*1000;S.transitionQuestion=serverQuestion;clearInterval(S.timer);render();startQuestionTransition();return;}
      S.transitionUntil=0;S.transitionQuestion=-1;render();
      if(S.route==='game'&&newStarted>0)startClock();
      return;
    }
    if(newStarted<=0 && S.route==='game' && sb && S.matchId){
      const {data:startedData}=await sb.rpc('start_match_round',{p_match_id:S.matchId});
      const startedAgain=Number(startedData?.state?.question_started_at||0);
      if(startedAgain>0){S.match=startedData;S.roundStartedAt=startedAgain*1000;render();startClock();return;}
    }
    S.roundStartedAt=newStarted*1000;
    const key=`${S.user.id}:${serverQuestion}`;
    S.answered=!!m.answers?.[key];S.waiting=S.answered;
    if(S.answered)clearInterval(S.timer);else if(newStarted>0)startClock();
    render();
  }
  function getScore(id){return Number(S.match?.scores?.[id]||0)}
  function totalTeam(team){return(team||[]).reduce((n,id)=>n+getScore(id),0)}
  function myTeam(){return(S.match?.team_a||[]).includes(S.user.id)?S.match.team_a:S.match?.team_b||[S.user.id]}
  function opponentInfo(){const ids=(S.match?.player_ids||[]).filter(id=>id!==S.user.id);const id=ids[0];return id?(S.matchPlayers[id]||{id,username:'Oponente'}):(S.bot||{id:'bot',username:'Robô',display_name:'Robô'});}
  function startQuestionTransition(){clearTimeout(S.questionTransitionTimer);const run=()=>{if(S.route!=='game')return;const left=Math.max(0,S.transitionUntil-Date.now());if(left<=0){S.transitionUntil=0;S.transitionQuestion=-1;render();startClock();return}render();S.questionTransitionTimer=setTimeout(run,250)};run();}
  function asyncOpponent(){const id=(S.match?.player_ids||[]).find(x=>x!==S.user.id);return id?(S.matchPlayers[id]||{id,username:'Amigo'}):{id:'opponent',username:'Amigo'};}
  function asyncProgressOf(id=S.user.id){return Number(S.match?.player_progress?.[id]||0)}
  function asyncAnswerFor(qi=S.questionIndex){return S.match?.answers?.[`${S.user.id}:${qi}`];}
  function asyncGame(){
    const myProgress=asyncProgressOf();
    if(myProgress>=7){return `<div class="classic-loading async-waiting"><div class="loading-bolt">ϟ</div><b>DESAFIO CONCLUÍDO</b><h2>⏳ Aguardando seu amigo</h2><p class="muted">Você terminou suas 7 perguntas. O resultado será liberado quando seu amigo também terminar.</p><div class="async-score-wait">Sua pontuação: <strong>${getScore(S.user.id)}</strong></div><button class="secondary" id="asyncBackFriends">VOLTAR AOS AMIGOS</button></div>`}
    const q=S.questions[myProgress];if(!q)return '<div class="card pad center">Pergunta indisponível.</div>';
    const mine=getScore(S.user.id),opp=asyncOpponent(),rival=getScore(opp.id),myAnswer=asyncAnswerFor(myProgress);
    return `<div class="classic-game async-game"><div class="classic-scorebar"><div>${av(S.profile?.display_name||'V','avatar tiny',S.profile?.avatar_url||'')}<span>${esc(S.profile?.username||'Você')}</span><b>${mine}</b></div><div class="round-count">${myProgress+1}/7</div><div>${av(opp.username||'R','avatar tiny',opp.avatar_url||'')}<span>${esc(opp.username||'Amigo')}</span><b>${rival}</b></div></div><div class="classic-question"><div class="classic-timer" id="tm">${S.seconds}</div>${q.image_url?`<img src="${esc(q.image_url)}" alt="">`:''}<p>${esc(q.question_text||'')}</p><i class="quiz-line left"></i><i class="quiz-line right"></i></div><div class="classic-answers">${(q.options||[]).map((a,i)=>`<button class="classic-answer ${myAnswer&&Number(myAnswer.answer)===i?'selected':''} ${myAnswer&&Number(myAnswer.answer)===i?(myAnswer.correct?'answer-correct':'answer-wrong'):''}" data-async-a="${i}" ${S.asyncAnswered?'disabled':''}><span>${'ABCD'[i]}</span>${esc(a)}${myAnswer&&Number(myAnswer.answer)===i?`<b>${myAnswer.correct?'✓':'✕'}</b>`:''}</button>`).join('')}</div>${S.asyncAnswered?'<div class="classic-wait">RESPOSTA ENVIADA</div>':''}<div class="async-note">Desafio assíncrono • responda no seu ritmo</div></div>`;
  }
  function startAsyncClock(){
    clearInterval(S.timer);const token=++S.clockToken;S.lastClockSecond=null;
    if(S.asyncAnswered||S.route!=='async-game')return;
    const started=Number(S.asyncStartedAt||0)*1000;
    if(!started){S.seconds=10;const el=$('#tm');if(el)el.textContent='10';return;}
    const tick=()=>{if(token!==S.clockToken||S.asyncAnswered||S.route!=='async-game'){clearInterval(S.timer);return;}const now=Date.now()+Number(S.clockOffsetMs||0);const remaining=Math.max(0,10-Math.floor(Math.max(0,now-started)/1000));S.seconds=remaining;const el=$('#tm');if(el)el.textContent=String(remaining);if(S.lastClockSecond!==remaining){S.lastClockSecond=remaining;if(remaining>0&&remaining<=3)sound('count')}if(remaining===0){clearInterval(S.timer);if(!S.asyncAnswered&&!S.timeoutInFlight)submitAsyncAnswer(-1)}};
    tick();S.timer=setInterval(tick,100);
    if(sb&&S.matchId){const before=Date.now();sb.rpc('get_match_clock',{p_match_id:S.matchId}).then(({data,error})=>{const after=Date.now();if(error||token!==S.clockToken)return;const serverNow=Number(data?.server_now||0)*1000;if(serverNow){S.clockOffsetMs=serverNow-(before+(after-before)/2);tick()}}).catch(()=>{})}
  }
  async function openAsyncChallenge(challengeId){
    if(!sb)return alert('Supabase não está configurado.');
    clearInterval(S.searchTimer);clearTimeout(S.poll);S.searching=false;S.asyncMode=true;S.asyncChallengeId=challengeId;
    const {data:c,error:ce}=await sb.from('challenges').select('id,challenger_id,challenged_id,category,status,match_id').eq('id',challengeId).single();
    if(ce||!c||!c.match_id)return alert(ce?.message||'Desafio indisponível.');
    S.matchId=c.match_id;const {data:m,error:me}=await sb.from('matches').select('*').eq('id',c.match_id).single();
    if(me||!m)return alert(me?.message||'Partida do desafio não encontrada.');
    S.match=m;S.mode='1v1';S.category=m.category;S.botMode=false;S.bot=null;S.resultSoundPlayed=false;S.rematchMessage='';S.transitionUntil=0;S.transitionQuestion=-1;S.matchPlayers={};
    S.questionIndex=asyncProgressOf();S.asyncProgress=S.questionIndex;S.asyncAnswered=!!m.answers?.[`${S.user.id}:${S.questionIndex}`];S.answered=S.asyncAnswered;S.waiting=false;S.timeoutInFlight=false;S.clockOffsetMs=0;S.asyncStartedAt=Number(m.player_started_at?.[S.user.id]||0);await loadMatchPlayers(m);await loadMatchQuestions();
    await startAsyncRound();subscribeAsyncMatch();S.route='async-game';render();if(!S.asyncAnswered&&S.asyncStartedAt)startAsyncClock();
  }
  async function startAsyncRound(){if(!sb||!S.matchId)return;const {data,error}=await sb.rpc('start_async_challenge',{p_match_id:S.matchId});if(error)return alert(error.message);if(data){S.match=data;S.asyncProgress=asyncProgressOf();S.questionIndex=S.asyncProgress;S.asyncStartedAt=Number(data.player_started_at?.[S.user.id]||0);S.asyncAnswered=!!data.answers?.[`${S.user.id}:${S.asyncProgress}`];S.answered=S.asyncAnswered;}}
  function subscribeAsyncMatch(){if(!sb||!S.matchId)return;if(S.realtime)sb.removeChannel(S.realtime);S.realtime=sb.channel(`async-match-${S.matchId}`).on('postgres_changes',{event:'UPDATE',schema:'public',table:'matches',filter:`id=eq.${S.matchId}`},async payload=>syncAsyncMatch(payload.new)).subscribe();clearInterval(S.matchSyncTimer);S.matchSyncTimer=setInterval(async()=>{if(S.route!=='async-game'||!S.matchId)return;const {data}=await sb.from('matches').select('*').eq('id',S.matchId).single();if(data)await syncAsyncMatch(data)},1000)}
  async function syncAsyncMatch(m){if(!m||!S.asyncMode)return;S.match=m;S.category=m.category;S.mode=m.mode;if(m.status==='finished'){clearInterval(S.timer);clearInterval(S.matchSyncTimer);S.matchSyncTimer=null;await finishAsyncOnline();return}const progress=asyncProgressOf();if(progress!==S.asyncProgress){S.asyncProgress=progress;S.questionIndex=progress;S.asyncAnswered=!!m.answers?.[`${S.user.id}:${progress}`];S.answered=S.asyncAnswered;S.asyncStartedAt=Number(m.player_started_at?.[S.user.id]||0);S.seconds=10;S.lastClockSecond=null;render();if(!S.asyncAnswered&&S.asyncStartedAt)startAsyncClock();return}if(progress<7&&!S.asyncAnswered){const started=Number(m.player_started_at?.[S.user.id]||0);if(started){S.asyncStartedAt=started;startAsyncClock()}else{await startAsyncRound();render();if(S.asyncStartedAt)startAsyncClock()}}}
  async function submitAsyncAnswer(i){if(S.asyncAnswered||S.timeoutInFlight||S.route!=='async-game')return;const qi=S.asyncProgress;const q=S.questions[qi];if(!q)return;S.asyncAnswered=true;clearInterval(S.timer);S.seconds=i===-1?0:S.seconds;if(i===-1)sound('timeout');else sound(i===Number(q.correct_index)?'correct':'wrong');render();const {data,error}=await sb.rpc('submit_async_challenge_answer',{p_match_id:S.matchId,p_question_index:qi,p_answer_index:i});if(error){S.asyncAnswered=false;render();alert(error.message);return}if(data?.finished){const {data:m}=await sb.from('matches').select('*').eq('id',S.matchId).single();if(m)await syncAsyncMatch(m);return}const {data:m}=await sb.from('matches').select('*').eq('id',S.matchId).single();if(m){S.match=m;S.asyncProgress=Number(m.player_progress?.[S.user.id]||qi+1);S.questionIndex=S.asyncProgress;S.asyncAnswered=false;S.asyncStartedAt=0;S.seconds=10;render();await startAsyncRound();render();if(S.asyncStartedAt)startAsyncClock()}}
  async function finishAsyncOnline(){if(S.route==='result')return;const mine=getScore(S.user.id);const opp=(S.match?.player_ids||[]).find(x=>x!==S.user.id);const oppScore=getScore(opp);const win=mine>oppScore;if(sb){const {data:existing}=await sb.from('game_results').select('id').eq('match_id',S.matchId).eq('user_id',S.user.id).maybeSingle();if(!existing){await sb.from('game_results').insert({user_id:S.user.id,category:S.category,mode:'1v1',score:mine,won:win,match_id:S.matchId});await sb.rpc('apply_game_result',{p_user_id:S.user.id,p_category:S.category,p_score:mine,p_won:win});await loadProfile()}}S.myScore=mine;S.rankRows=null;S.profileDetails=null;S.route='result';render()}

  function game(){
    if(S.transitionUntil>Date.now()){const n=Math.max(1,Math.ceil((S.transitionUntil-Date.now())/1000));return `<div class="classic-round-transition"><div class="loading-bolt">ϟ</div><b>RODADA ${S.questionIndex+1}</b><strong>${n}</strong></div>`}
    const q=S.questions[S.questionIndex];if(!q)return '<div class="card pad center"><h2>Pergunta indisponível</h2></div>';const mine=getScore(S.user.id),opp=opponentInfo(),rival=getScore(opp.id),progress=S.questionIndex+1;const answerMap=S.match?.answers||{},myAnswer=answerMap[`${S.user.id}:${S.questionIndex}`];
    return `<div class="classic-game"><div class="classic-scorebar"><div>${av(S.profile?.display_name||'V','avatar tiny',S.profile?.avatar_url||'')}<span>${esc(S.profile?.username||'Você')}</span><b>${mine}</b></div><div class="round-count">${progress}/7</div><div>${av(opp.username||'R','avatar tiny',opp.avatar_url||'')}<span>${esc(opp.username||'Oponente')}</span><b>${rival}</b></div></div><div class="classic-question"><div class="classic-timer" id="tm">${S.seconds}</div>${q.image_url?`<img src="${esc(q.image_url)}" alt="">`:''}<p>${esc(q.question_text||'')}</p><i class="quiz-line left"></i><i class="quiz-line right"></i></div><div class="classic-answers">${(q.options||[]).map((a,i)=>`<button class="classic-answer ${myAnswer&&Number(myAnswer.answer)===i?'selected':''} ${myAnswer&&Number(myAnswer.answer)===i?(myAnswer.correct?'answer-correct':'answer-wrong'):''}" data-a="${i}" ${S.answered?'disabled':''}><span>${'ABCD'[i]}</span>${esc(a)}${myAnswer&&Number(myAnswer.answer)===i?`<b>${myAnswer.correct?'✓':'✕'}</b>`:''}</button>`).join('')}</div>${S.waiting?'<div class="classic-wait">RESPOSTA ENVIADA • AGUARDANDO O OPONENTE</div>':''}</div>`;
  }


  function startClock(){
    clearInterval(S.timer);
    const token=++S.clockToken;
    const syncToken=++S.clockSyncToken;
    S.lastClockSecond=null;
    if(S.answered||S.waiting||S.route!=='game')return;
    const started=Number(S.roundStartedAt||Number(S.match?.state?.question_started_at||0)*1000);
    if(!Number.isFinite(started)||started<=0){
      S.seconds=10;const el=$('#tm');if(el)el.textContent='10';return;
    }
    S.roundStartedAt=started;S.timeLimit=10;
    const tick=()=>{
      if(token!==S.clockToken||S.answered||S.waiting||S.route!=='game'){clearInterval(S.timer);return;}
      const now=Date.now()+Number(S.clockOffsetMs||0);
      const elapsed=Math.max(0,now-started);
      const remaining=Math.max(0,10-Math.floor(elapsed/1000));
      S.seconds=remaining;
      const el=$('#tm');if(el)el.textContent=String(remaining);
      if(S.lastClockSecond!==remaining){
        S.lastClockSecond=remaining;
        if(remaining>0&&remaining<=3)sound('count');
      }
      if(remaining===0){
        clearInterval(S.timer);
        if(!S.answered&&!S.waiting&&!S.timeoutInFlight)timeoutRound();
      }
    };
    tick();
    S.timer=setInterval(tick,100);
    // Calibra o relógio local contra o relógio oficial do Supabase.
    // O timestamp da pergunta continua sendo o timestamp oficial da partida.
    if(sb&&S.matchId){
      const before=Date.now();
      sb.rpc('get_match_clock',{p_match_id:S.matchId}).then(({data,error})=>{
        if(error||syncToken!==S.clockSyncToken||token!==S.clockToken)return;
        const after=Date.now();
        const serverNow=Number(data?.server_now||0)*1000;
        if(serverNow>0){
          const midpoint=before+(after-before)/2;
          S.clockOffsetMs=serverNow-midpoint;
          tick();
        }
      }).catch(()=>{});
    }
  }
  async function timeoutRound(){
    if(S.answered||S.waiting||S.timeoutInFlight||S.route!=='game')return;
    S.timeoutInFlight=true;S.answered=true;S.seconds=0;clearInterval(S.timer);sound('timeout');
    const qi=S.questionIndex;
    const key=`${S.user.id}:${qi}`;
    // Visual state immediately, but the authoritative timeout is recorded by Supabase.
    S.match={...S.match,answers:{...(S.match?.answers||{}),[key]:{answer:-1,correct:false,score:0,remaining:0}}};
    render();
    if(!sb||!S.matchId){S.timeoutInFlight=false;return;}
    const {data,error}=await sb.rpc('submit_match_answer',{p_match_id:S.matchId,p_question_index:qi,p_answer_index:-1,p_seconds:0});
    S.timeoutInFlight=false;
    if(error){
      S.answered=false;
      delete (S.match?.answers||{})[key];
      const {data:m}=await sb.from('matches').select('*').eq('id',S.matchId).single();
      if(m)await syncMatch(m);
      return;
    }
    if(data?.resync||data?.finished||data?.next_question!==undefined){
      const {data:m}=await sb.from('matches').select('*').eq('id',S.matchId).single();
      if(m)await syncMatch(m);
      return;
    }
    S.waiting=true;render();
  }
  async function answer(i){
    if(S.answered||S.waiting)return;
    if(i<0){timeoutRound();return;}
    S.answered=true;clearInterval(S.timer);
    const seconds=Math.max(0,S.seconds);
    const currentQ=S.questions[S.questionIndex];
    if(i>=0 && currentQ){sound(i===Number(currentQ.correct_index)?'correct':'wrong');S.match={...S.match,answers:{...(S.match.answers||{}),[`${S.user.id}:${S.questionIndex}`]:{answer:i,correct:i===Number(currentQ.correct_index),score:0}}};render()}
    if(S.botMode){
      const q=S.questions[S.questionIndex];const correct=i===Number(q.correct_index);const remaining=Math.min(Math.max(seconds,0),10);const added=correct?(S.questionIndex===6?20+remaining*2:10+remaining):0;const myKey=`${S.user.id}:${S.questionIndex}`;S.match.scores[S.user.id]=getScore(S.user.id)+added;S.match.answers[myKey]={answer:i,correct,score:added};S.lastAdded=added;render();
      if(S.match.answers?.[`${S.bot.id}:${S.questionIndex}`]){setTimeout(maybeAdvanceBotRound,700)}else scheduleBotTurn();return;
    }
    if(!sb){delete (S.match?.answers||{})[`${S.user.id}:${S.questionIndex}`];S.answered=false;alert('Supabase não está configurado. Configure o config.js.');return go('login')}
    // Confirma o estado atual no servidor antes de enviar a resposta.
    const {data:latest,error:latestError}=await sb.from('matches').select('*').eq('id',S.matchId).single();
    if(latestError||!latest){delete (S.match?.answers||{})[`${S.user.id}:${S.questionIndex}`];S.answered=false;render();startClock();return alert(latestError?.message||'Não foi possível sincronizar a partida.')}
    if(latest.status==='finished'||Number(latest.current_question||0)!==Number(S.questionIndex||0)){
      await syncMatch(latest);return;
    }
    const {data,error}=await sb.rpc('submit_match_answer',{p_match_id:S.matchId,p_question_index:S.questionIndex,p_answer_index:i,p_seconds:seconds});
    if(error){
      // O servidor não deve mais exibir o alerta "question out of sync"; ainda assim,
      // se uma versão antiga da função RPC estiver instalada, sincronizamos silenciosamente.
      const msg=String(error.message||'');
      if(/question out of sync|match is not playing/i.test(msg)){
        delete (S.match?.answers||{})[`${S.user.id}:${S.questionIndex}`];
        const {data:m}=await sb.from('matches').select('*').eq('id',S.matchId).single();
        if(m){S.answered=false;await syncMatch(m);return}
      }
      delete (S.match?.answers||{})[`${S.user.id}:${S.questionIndex}`];S.answered=false;render();alert(msg);startClock();return;
    }
    S.timeoutInFlight=false;S.lastAdded=Number(data?.added||0);if(data?.resync||data?.finished){const {data:m}=await sb.from('matches').select('*').eq('id',S.matchId).single();if(m)await syncMatch(m);return}
    S.waiting=true;S.match={...S.match,answers:{...(S.match.answers||{}),[`${S.user.id}:${S.questionIndex}`]:{answer:i,correct:i===Number(currentQ?.correct_index),score:data?.added||0}}};clearInterval(S.timer);render()
  }
  function scheduleBotTurn(){if(!S.botMode||S.route!=='game'||S.answered||S.waiting)return;clearTimeout(S.botTurnTimer);const wait=1800+Math.random()*6500;S.botTurnTimer=setTimeout(()=>{if(S.transitionUntil>Date.now()){S.botTurnTimer=setTimeout(scheduleBotTurn,Math.max(250,S.transitionUntil-Date.now()+100));return}if(S.botMode&&S.route==='game'&&!S.match.answers?.[`${S.bot.id}:${S.questionIndex}`])botAnswer()},wait)}
  function maybeAdvanceBotRound(){if(!S.botMode||!S.match)return;const myKey=`${S.user.id}:${S.questionIndex}`;const botKey=`${S.bot.id}:${S.questionIndex}`;if(!S.match.answers?.[myKey]||!S.match.answers?.[botKey])return;if(S.questionIndex>=S.questions.length-1){finishBot();return}const next=S.questionIndex+1;S.questionIndex=next;S.match.current_question=next;S.match.state.question_started_at=(Date.now()+3000)/1000;S.transitionUntil=Date.now()+3000;S.transitionQuestion=next;S.answered=false;S.waiting=false;S.fiftyUsed=false;S.plusUsed=false;S.timeLimit=10;S.seconds=10;S.lastAdded=0;render();startQuestionTransition();scheduleBotTurn()}
  function botAnswer(){
    if(!S.botMode||S.route!=='game'||!S.match||S.transitionUntil>Date.now())return;
    const q=S.questions[S.questionIndex];if(!q)return;
    const key=`${S.bot.id}:${S.questionIndex}`;if(S.match.answers?.[key]){maybeAdvanceBotRound();return}
    const botId=S.bot.id;const correct=Math.random()<0.72;const answer=correct?Number(q.correct_index):[0,1,2,3].filter(x=>x!==Number(q.correct_index))[Math.floor(Math.random()*3)];const botSeconds=correct?(2+Math.floor(Math.random()*8)):10;const botRemaining=Math.min(Math.max(10-botSeconds,0),10);const added=correct?(S.questionIndex===6?20+botRemaining*2:10+botRemaining):0;
    S.match.scores[botId]=getScore(botId)+added;S.match.answers[key]={answer,correct,score:added};
    if(S.answered){S.waiting=false;render();setTimeout(maybeAdvanceBotRound,700)}else{render();}
  }
  async function startBotMatch(){
    clearTimeout(S.poll);S.poll=null;clearInterval(S.searchTimer);if(sb)await sb.rpc('cancel_match_queue');
    const {data,error}=await sb.from('questions').select('id,question_text,options,correct_index,image_url').eq('active',true).eq('approval_status','approved').eq('category_name',S.category).limit(50);
    if(error||!data||data.length<7){return alert('Esta categoria ainda não possui 7 perguntas ativas.')}
    const questions=[...data].sort(()=>Math.random()-.5).slice(0,7);const botId='00000000-0000-0000-0000-000000000001';S.bot={id:botId,username:'QuizBot',display_name:'QuizBot',avatar_url:''};S.botMode=true;S.mode='1v1';S.searching=false;S.botOffer=false;S.questions=questions;S.resultSoundPlayed=false;S.match={id:null,mode:'1v1',category:S.category,player_ids:[S.user.id,botId],team_a:[S.user.id],team_b:[botId],scores:{[S.user.id]:0,[botId]:0},answers:{},state:{question_started_at:null},status:'playing',current_question:0,question_ids:questions.map(q=>q.id)};S.matchPlayers={[botId]:S.bot};S.questionIndex=0;S.answered=false;S.waiting=false;startMatchCountdown();
  }
  async function finishBot(){
    clearInterval(S.timer);const mine=getScore(S.user.id),botScore=getScore(S.bot.id),win=mine>botScore;S.myScore=mine;S.match.status='finished';
    // Partidas contra robô não geram XP, vitórias ou derrotas no ranking.
    S.myScore=mine;
    S.route='result';render();
  }
  async function finishOnline(){if(S.route==='result')return;const mine=getScore(S.user.id);const teamA=totalTeam(S.match.team_a),teamB=totalTeam(S.match.team_b);const win=S.mode==='2v2'?(S.match.team_a.includes(S.user.id)?teamA>teamB:teamB>teamA):mine>Math.max(...S.match.player_ids.filter(x=>x!==S.user.id).map(getScore),0);if(sb){const {data:existing}=await sb.from('game_results').select('id').eq('match_id',S.matchId).eq('user_id',S.user.id).maybeSingle();if(!existing){await sb.from('game_results').insert({user_id:S.user.id,category:S.category,mode:S.mode,score:mine,won:win,match_id:S.matchId});await sb.rpc('apply_game_result',{p_user_id:S.user.id,p_category:S.category,p_score:mine,p_won:win});await loadProfile();}}S.myScore=mine;S.rankRows=null;S.profileDetails=null;S.route='result';render()}
  function startResultPresence(){clearInterval(S.resultPresenceTimer);if(!sb||!S.matchId||S.botMode)return;const beat=async()=>{if(S.route!=='result'||!S.matchId)return;const {data}=await sb.rpc('heartbeat_result_presence',{p_match_id:S.matchId});if(data?.rematch_match_id){clearInterval(S.resultPresenceTimer);S.resultPresenceTimer=null;await openMatch(data.rematch_match_id)}};beat();S.resultPresenceTimer=setInterval(beat,2000)}
  async function requestRematch(){if(!sb||!S.matchId||S.botMode)return;S.rematchMessage='';render();const {data,error}=await sb.rpc('rematch_match',{p_match_id:S.matchId});if(error){S.rematchMessage=error.message;render();return}if(data?.ok&&data.match_id){clearInterval(S.resultPresenceTimer);S.resultPresenceTimer=null;await openMatch(data.match_id);return}S.rematchMessage='Oponente se retirou da sala.';render()}
  function result(){
    const mine=getScore(S.user.id)||S.myScore;const opponents=(S.match?.player_ids||[]).filter(x=>x!==S.user.id);const opp=opponents[0]?S.matchPlayers[opponents[0]]:(S.bot||{});const oppScore=S.mode==='2v2'?((S.match?.team_a||[]).includes(S.user.id)?totalTeam(S.match.team_b):totalTeam(S.match.team_a)):(opponents[0]?getScore(opponents[0]):getScore(S.bot?.id));if(!S.resultSoundPlayed){sound(mine>oppScore?'win':'lose');S.resultSoundPlayed=true}if(sb&&S.matchId&&!S.botMode&&!S.resultPresenceTimer)setTimeout(startResultPresence,0);const win=mine>oppScore,perfect=mine===160;
    const chart=detailChart();
    return `<div class="classic-result"><div class="result-kicker">RESULTADOS</div><h1>${win?'VITÓRIA':'DERROTA'}</h1><div class="result-players"><div>${av(S.profile?.display_name||'J','avatar big',S.profile?.avatar_url||'')}<b>${esc(S.profile?.username||'Você')}</b><strong class="${win?'green':'red'}">${mine}</strong></div><span>VS</span><div>${av(opp?.username||'R','avatar big',opp?.avatar_url||'')}<b>${esc(opp?.username||'Oponente')}</b><strong class="red">${oppScore}</strong></div></div><div class="result-metrics"><div><b>${mine}</b><small>PONTUAÇÃO</small></div><div><b>${S.match?.answers?Object.values(S.match.answers).filter(x=>x&&x.score).length:0}</b><small>RESPOSTAS</small></div><div><b>${perfect?'160':'+'+mine}</b><small>XP DA PARTIDA</small></div></div>${perfect?'<div class="perfect-score">🏆 PONTUAÇÃO PERFEITA!</div>':''}<div class="classic-xp"><div class="xp-circle"><span>XP</span><b>${S.botMode?'+0':'+'+mine}</b></div><div><b>${S.botMode?'Partida contra robô':'Experiência conquistada'}</b><small>${win?'Você venceu este confronto.':'Continue jogando para subir no ranking.'}</small></div></div><div class="result-detail"><b>DETALHES</b>${chart}</div><div class="result-actions">${!S.botMode&&opponents.length?`<button class="result-btn play" id="rematch">Jogar</button><button class="result-btn chat" id="resultChat">Chat</button><button class="result-btn publish" id="publishResult">Publicar</button>`:''}<button class="secondary" id="again">JOGAR OUTRA</button><button class="secondary" id="home">VOLTAR AO INÍCIO</button>${S.rematchMessage?`<div class="notice center rematch-notice">${esc(S.rematchMessage)}</div><button class="secondary" id="findAnother">PROCURAR OUTRO</button>`:''}</div></div>`;
  }
  function detailChart(){const ids=[S.user.id,...((S.match?.player_ids||[]).filter(x=>x!==S.user.id).slice(0,1))];const pts=ids.map(id=>{let total=0;return Array.from({length:7},(_,i)=>{const a=S.match?.answers?.[`${id}:${i}`];total+=Number(a?.score||0);return total})});const max=160;const make=(arr)=>arr.map((v,i)=>`${20+i*50},${150-(v/max*120)}`).join(' ');return `<svg class="score-chart" viewBox="0 0 340 175" role="img"><line x1="20" y1="150" x2="320" y2="150"/><line x1="20" y1="30" x2="20" y2="150"/><polyline points="${make(pts[0]||[])}"/><polyline class="rival-line" points="${make(pts[1]||[])}"/><text x="20" y="168">1</text><text x="70" y="168">2</text><text x="120" y="168">3</text><text x="170" y="168">4</text><text x="220" y="168">5</text><text x="270" y="168">6</text><text x="315" y="168">7</text></svg>`}


  function rank(){if(!S.rankRows){setTimeout(loadRanking,0);return `<section class="classic-page"><div class="classic-banner"><b>Ranking</b><span>Veja quem está dominando cada tópico.</span></div><div class="card pad center">Carregando ranking...</div></section>`}const rows=S.rankRows||[];const selected=S.rankCategory;return `<section class="classic-page"><div class="classic-section-head"><b>Ranking</b><span class="muted">${selected==='__global__'?'Global':esc(selected)}</span></div><select id="rankCategory" class="classic-select"><option value="__global__" ${selected==='__global__'?'selected':''}>Ranking Global</option>${(S.categories||[]).map(c=>`<option value="${esc(c.name)}" ${selected===c.name?'selected':''}>${esc(c.name)}</option>`).join('')}</select><div class="tabs classic-tabs"><button class="on">Esta temporada</button><button>Todos</button><button>Amigos</button></div><div class="podium">${rows.slice(0,3).map((r,i)=>`<button class="podium-card p${i+1}" data-profile="${r.id}"><span>${['🥇','🥈','🥉'][i]}</span>${av(r.username||'J','avatar mini',r.avatar_url||'')}<b>${esc(r.username||r.display_name||'Jogador')}</b><small>${selected==='__global__'?(r.xp||0)+' XP':'Nível '+(r.level||1)}</small></button>`).join('')}</div><div class="list">${rows.slice(3).map((r,i)=>`<button class="row player-row ${r.id===S.user.id?'me':''}" data-profile="${r.id}"><b>#${i+4}</b>${av(r.username||'J','avatar mini',r.avatar_url||'')}<div><b>${esc(r.username||r.display_name||'Jogador')}</b><div class="muted">Nível ${r.level||1} • ${r.wins||0} vitórias</div></div><strong>${selected==='__global__'?(r.xp||0)+' XP':(r.topic_xp||0)+' XP'}</strong></button>`).join('')}</div></section>`}
  async function loadRanking(){if(!sb)return;if(S.rankCategory&&S.rankCategory!=='__global__'){const {data,error}=await sb.rpc('get_topic_ranking',{p_category:S.rankCategory,p_limit:100});if(error)return alert(error.message);S.rankRows=data||[];render();return}const {data,error}=await sb.from('profiles').select('id,username,display_name,avatar_url,xp,wins,level').order('xp',{ascending:false}).limit(100);if(error)return alert(error.message);S.rankRows=data||[];render()}

  function profile(){if(!S.recentRows)setTimeout(loadRecentResults,0);const id=S.profileTargetId||S.user.id;if(!S.profileDetails||S.profileDetails.id!==id){setTimeout(()=>loadProfileDetails(id),0);return `<section class="classic-page"><div class="card pad center">Carregando perfil...</div></section>`}const p=S.profileDetails,mine=id===S.user.id;const topics=(S.recentRows||[]).filter(r=>r&&r.category).reduce((a,r)=>{a[r.category]=(a[r.category]||0)+Number(r.score||0);return a},{});const topTopics=Object.entries(topics).sort((a,b)=>b[1]-a[1]).slice(0,6);return `<section class="classic-profile"><div class="profile-cover">${av(p.username||'J','avatar profile-avatar',p.avatar_url||'')}<h1>${esc(p.username||p.display_name)}</h1><p>Nível ${p.level||1} • ${p.xp||0} XP</p><div class="profile-numbers"><span><b>${p.games||0}</b>JOGOS</span><span><b>${p.wins||0}</b>VITÓRIAS</span><span><b>${p.losses||0}</b>DERROTAS</span></div></div>${mine?'<label class="photo-btn profile-photo-action" for="avatarFile">📷 Alterar foto</label><input id="avatarFile" type="file" accept="image/*" class="hidden">':''}<div class="classic-section-head"><b>Meus tópicos</b><span>Mais jogados</span></div><div class="profile-topic-list">${topTopics.map(([name,xp])=>`<button data-topic="${esc(name)}"><span>✦</span><div><b>${esc(name)}</b><small>Nível ${Math.max(1,1+Math.floor(xp/1000))}</small></div><strong>${xp} XP</strong></button>`).join('')||'<div class="classic-empty">Jogue para construir seu perfil por tópico.</div>'}</div><div class="profile-grid classic-profile-grid"><div><span>Partidas</span><b>${p.games||0}</b></div><div><span>Vitórias</span><b>${p.wins||0}</b></div><div><span>Derrotas</span><b>${p.losses||0}</b></div><div><span>Taxa de vitória</span><b>${p.accuracy||0}%</b></div></div>${mine?'<button class="secondary" id="friendsBtn">♧ MEUS AMIGOS</button>':'<button class="secondary" id="backRank">← VOLTAR AO RANKING</button>'}</section>`}


  async function loadProfileDetails(id){if(!sb){S.profileDetails={...S.profile,id,rank:1,mostPlayed:'Geral',games:S.profile?.wins||0,accuracy:0};return render()}const {data:p,error}=await sb.from('profiles').select('id,username,display_name,avatar_url,xp,level,wins,losses,streak').eq('id',id).single();if(error||!p){alert(error?.message||'Perfil não encontrado.');return go('home')}const [{data:all}, {data:results}]=await Promise.all([sb.from('profiles').select('id,xp').order('xp',{ascending:false}),sb.from('game_results').select('category,score,won').eq('user_id',id)]);const rows=all||[];const rank=(rows.findIndex(r=>r.id===id)+1)||'-';const games=(results||[]).length;const most=(results||[]).reduce((acc,r)=>(acc[r.category]=(acc[r.category]||0)+1,acc),{});const mostPlayed=Object.entries(most).sort((a,b)=>b[1]-a[1])[0]?.[0]||'Ainda não jogou';S.profileDetails={...p,rank,games,mostPlayed,accuracy:games?Math.round(((results||[]).filter(r=>r.won).length/games)*100):0};render()}
  async function uploadAvatar(file){if(!sb||!file)return;if(file.size>3*1024*1024)return alert('Escolha uma imagem de até 3 MB.');if(!file.type.startsWith('image/'))return alert('Escolha uma imagem.');const ext=(file.name.split('.').pop()||'jpg').toLowerCase().replace(/[^a-z0-9]/g,'')||'jpg';const path=`${S.user.id}/${Date.now()}.${ext}`;const {error}=await sb.storage.from('avatars').upload(path,file,{contentType:file.type,cacheControl:'3600'});if(error)return alert(error.message);const {data:urlData}=sb.storage.from('avatars').getPublicUrl(path);const {error:updateError}=await sb.from('profiles').update({avatar_url:urlData.publicUrl}).eq('id',S.user.id);if(updateError)return alert(updateError.message);S.profile.avatar_url=urlData.publicUrl;S.profileDetails=null;alert('Foto de perfil atualizada.');render()}
  function notificationsView(){const rows=S.notifications||[];return `<div class="title">🔔 Notificações</div><div class="subtitle">Desafios, novos amigos e mensagens recebidas.</div><div class="card pad notification-list">${rows.length?rows.map(n=>{const actor=S.notificationActors[n.actor_id];const icon=n.type==='challenge'?'⚔️':n.type==='friend_request'?'👥':n.type==='message'?'💬':'🔔';const unread=!n.read_at;return `<div class="notification-item ${unread?'unread':''}" data-notification-id="${n.id}"><div class="notification-avatar">${av(actor?.username||'J','avatar mini',actor?.avatar_url||'')}</div><div class="notification-content"><b>${icon} ${esc(n.title||'Nova notificação')}</b><div class="muted">${esc(n.body||'')}</div><small>${new Date(n.created_at).toLocaleString('pt-BR',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'})}</small></div>${unread?'<span class="unread-dot"></span>':''}</div>`}).join(''):'<div class="empty-notifications">✨ Você está em dia!<div class="muted">Nenhuma notificação nova.</div></div>'}</div><div class="notification-actions"><button class="secondary" id="markAllNotifications">MARCAR TODAS COMO LIDAS</button><button class="secondary" id="notificationsHome">← VOLTAR AO INÍCIO</button></div>`}
  function friends(){if(!S.friendRows){setTimeout(loadFriends,0);return `<div class="title">Amigos</div><div class="card pad center"><div class="muted">Carregando amizades...</div></div>`}const {list,people}=S.friendRows;const challenges=S.challengeRows||[];const term=S.challengeSearch.trim().toLowerCase();const cats=(S.categories||[]).filter(c=>!term||c.name.toLowerCase().includes(term));const mainCats=cats.filter(c=>!c.parent_id).slice(0,6);const otherCats=cats.filter(c=>c.parent_id||!mainCats.some(x=>x.id===c.id));const received=challenges.filter(c=>!c.mine);const sent=challenges.filter(c=>c.mine);return `<div class="title">Amigos</div><form id="friendSearch" class="searchbar"><input id="friendName" required placeholder="Nome de usuário exato"><button>ADICIONAR</button></form><div class="card pad"><div class="muted" style="margin-bottom:10px">Adicione pelo nome de usuário, converse ou desafie um amigo.</div>${list.length?list.map(f=>{const other=people[f.requester_id===S.user.id?f.addressee_id:f.requester_id];const accepted=f.status==='accepted';return `<div class="friend"><button class="plain-profile" data-profile="${other?.id||''}">${av(other?.username||'J','avatar mini',other?.avatar_url||'')}</button><div><b>${esc(other?.username||'Jogador')}</b><div class="muted">${accepted?'Amigo':f.addressee_id===S.user.id?'Solicitação recebida':'Solicitação enviada'}</div></div>${accepted?`<div style="display:flex;gap:5px"><button class="secondary chatFriend" data-id="${other?.id||''}" data-name="${esc(other?.username||'Jogador')}">💬</button><button class="secondary challengeFriend" data-id="${other?.id||''}" data-name="${esc(other?.username||'Jogador')}">⚔️</button></div>`:''}${f.status==='pending'&&f.addressee_id===S.user.id?`<button class="secondary acceptFriend" data-id="${f.id}">ACEITAR</button>`:''}</div>`}).join(''):'<div class="muted">Você ainda não tem amizades.</div>'}</div>${received.length?`<div class="card pad" style="margin-top:12px"><b>⚔️ Desafios recebidos</b>${received.map(c=>`<div class="friend"><div style="flex:1">${av(c.other_username||'J','avatar mini',c.other_avatar_url||'')}<b>${esc(c.other_username)}</b><div class="muted">te desafiou em <b>${esc(c.category)}</b> • Você pode jogar agora</div></div><button class="primary acceptChallenge" data-id="${c.id}">JOGAR DESAFIO</button></div>`).join('')}</div>`:''}${sent.length?`<div class="card pad" style="margin-top:12px"><b>📤 Meus desafios</b>${sent.map(c=>`<div class="friend"><div style="flex:1">${av(c.other_username||'J','avatar mini',c.other_avatar_url||'')}<b>${esc(c.other_username)}</b><div class="muted">${esc(c.category)} • ${c.status==='accepted'?'Seu amigo já começou':'Aguardando seu amigo'}</div></div>${c.match_id?`<button class="secondary continueChallenge" data-id="${c.id}">CONTINUAR</button>`:''}</div>`).join('')}</div>`:''}${S.challengeModal?`<div class="modal-backdrop"><div class="modal-card challenge-picker"><button class="modal-close" id="closeChallenge">×</button><div class="match-badge">⚔️ DESAFIAR ${esc((people[S.challengeTargetId]||{}).username||'AMIGO')}</div><h2>Escolha a categoria</h2><p class="muted">Seu amigo não precisa estar online. Você joga primeiro e ele responde depois.</p><div class="searchbar"><input id="challengeSearch" value="${esc(S.challengeSearch)}" placeholder="🔎 Pesquisar categoria..."></div><div class="challenge-cats">${(term?cats:mainCats).map(c=>`<button class="challenge-cat" data-challenge-cat="${esc(c.name)}"><span>${esc(c.icon||'🌐')}</span><b>${esc(c.name)}</b><small>${c.parent_id?'Subcategoria':'Principal'}</small></button>`).join('')||'<div class="notice">Nenhuma categoria encontrada.</div>'}</div>${!term&&otherCats.length?`<details class="more-cats"><summary>Ver outras categorias</summary><div class="challenge-cats">${otherCats.map(c=>`<button class="challenge-cat" data-challenge-cat="${esc(c.name)}"><span>${esc(c.icon||'🌐')}</span><b>${esc(c.name)}</b><small>${c.parent_id?'Subcategoria':'Principal'}</small></button>`).join('')}</div></details>`:''}</div></div>`:''}<button class="secondary" id="friendsHome">← VOLTAR AO INÍCIO</button>`}
  function subscribeChallenges(){if(!sb||!S.user)return;if(S.challengeRealtime)sb.removeChannel(S.challengeRealtime);S.challengeRealtime=sb.channel(`user-challenges-${S.user.id}`).on('postgres_changes',{event:'UPDATE',schema:'public',table:'challenges',filter:`challenger_id=eq.${S.user.id}`},async payload=>{const c=payload.new;if(c.status==='accepted'&&c.match_id){S.friendRows=null;if(S.route==='friends')render()}}).subscribe()}
  async function loadNotifications(){if(!sb||!S.user)return;const {data,error}=await sb.from('notifications').select('id,recipient_id,actor_id,type,title,body,data,read_at,created_at').eq('recipient_id',S.user.id).order('created_at',{ascending:false}).limit(50);if(error){console.error(error);return}S.notifications=data||[];S.notificationUnread=S.notifications.filter(n=>!n.read_at).length;const ids=[...new Set(S.notifications.map(n=>n.actor_id).filter(Boolean))];if(ids.length){const {data:pdata}=await sb.from('profiles').select('id,username,avatar_url').in('id',ids);S.notificationActors={};(pdata||[]).forEach(p=>S.notificationActors[p.id]=p)}S.notificationsLoaded=true}
  function subscribeNotifications(){if(!sb||!S.user)return;if(S.notificationRealtime)sb.removeChannel(S.notificationRealtime);S.notificationRealtime=sb.channel(`notifications-${S.user.id}`).on('postgres_changes',{event:'INSERT',schema:'public',table:'notifications',filter:`recipient_id=eq.${S.user.id}`},async payload=>{const n=payload.new;if(!S.notifications.some(x=>x.id===n.id)){S.notifications.unshift(n);S.notificationUnread++;const actorId=n.actor_id;if(actorId){const {data:p}=await sb.from('profiles').select('id,username,avatar_url').eq('id',actorId).maybeSingle();if(p)S.notificationActors[p.id]=p}sound('click');render()}}).on('postgres_changes',{event:'UPDATE',schema:'public',table:'notifications',filter:`recipient_id=eq.${S.user.id}`},payload=>{const i=S.notifications.findIndex(x=>x.id===payload.new.id);if(i>=0){S.notifications[i]=payload.new;S.notificationUnread=S.notifications.filter(n=>!n.read_at).length;render()}}).subscribe()}
  async function markNotificationRead(id){if(!sb||!id)return;const {error}=await sb.from('notifications').update({read_at:new Date().toISOString()}).eq('id',id).eq('recipient_id',S.user.id);if(!error){const n=S.notifications.find(x=>x.id===id);if(n&&!n.read_at){n.read_at=new Date().toISOString();S.notificationUnread=Math.max(0,S.notificationUnread-1)}}}
  async function markAllNotifications(){if(!sb||!S.user)return;const {error}=await sb.from('notifications').update({read_at:new Date().toISOString()}).eq('recipient_id',S.user.id).is('read_at',null);if(error)return alert(error.message);S.notifications.forEach(n=>{if(!n.read_at)n.read_at=new Date().toISOString()});S.notificationUnread=0;render()}
  async function openNotification(n){if(!n)return;await markNotificationRead(n.id);if(n.type==='challenge'||n.type==='friend_request'){S.friendRows=null;go('friends');return}if(n.type==='system'&&n.data?.match_id){const {data:m}=await sb.from('matches').select('*').eq('id',n.data.match_id).maybeSingle();if(m?.match_kind==='async_friend'&&m.status==='finished'){S.asyncMode=true;S.asyncChallengeId=n.data.challenge_id||m.async_challenge_id;S.matchId=m.id;S.match=m;S.mode='1v1';S.category=m.category;await loadMatchPlayers(m);await loadMatchQuestions();await finishAsyncOnline();return}}if(n.type==='message'&&n.actor_id){const p=S.notificationActors[n.actor_id];if(p)await openChat(n.actor_id,p.username);else go('friends');return}render()}
  async function loadFriends(){if(!sb)return;const {data,error}=await sb.from('friendships').select('id,requester_id,addressee_id,status,created_at').or(`requester_id.eq.${S.user.id},addressee_id.eq.${S.user.id}`).order('created_at',{ascending:false});if(error)return alert(error.message);const list=data||[];const ids=[...new Set(list.flatMap(f=>[f.requester_id,f.addressee_id]).filter(id=>id!==S.user.id))];let people={};if(ids.length){const {data:pdata}=await sb.from('profiles').select('id,username,avatar_url').in('id',ids);(pdata||[]).forEach(p=>people[p.id]=p)}const {data:ch}=await sb.from('challenges').select('id,challenger_id,challenged_id,category,status,match_id,created_at').or(`challenged_id.eq.${S.user.id},challenger_id.eq.${S.user.id}`).in('status',['pending','accepted']).order('created_at',{ascending:false});let challengeRows=ch||[];if(challengeRows.length){const ids2=[...new Set(challengeRows.map(x=>x.challenger_id===S.user.id?x.challenged_id:x.challenger_id))];const {data:cp}=await sb.from('profiles').select('id,username,avatar_url').in('id',ids2);const pm={};(cp||[]).forEach(x=>pm[x.id]=x);challengeRows=challengeRows.map(x=>({...x,other_username:pm[x.challenger_id===S.user.id?x.challenged_id:x.challenger_id]?.username||'Jogador',other_avatar_url:pm[x.challenger_id===S.user.id?x.challenged_id:x.challenger_id]?.avatar_url||'',mine:x.challenger_id===S.user.id}))}S.friendRows={list,people};S.challengeRows=challengeRows;render()}
  function achievements(){if(!S.achievementsRows){setTimeout(loadAchievements,0);return `<div class="title">Conquistas</div><div class="card pad center"><div class="muted">Carregando conquistas...</div></div>`}const rows=S.achievementsRows;return `<div class="title">Conquistas</div><div class="list">${rows.length?rows.map(a=>`<div class="row"><b style="font-size:24px">${esc(a.icon||'🏆')}</b><div><b>${esc(a.title)}</b><div class="muted">${esc(a.description||'')}</div></div></div>`).join(''):'<div class="card pad center">Nenhuma conquista publicada ainda.</div>'}</div>`}
  function admin(){
    if(!S.isAdmin)return '<div class="card pad"><h2>Acesso negado</h2></div>';
    const cats=S.categories.length?S.categories:catFallback();
    const qcats=['Todos',...cats.map(c=>c.name)];
    const filtered=S.adminQuestions.filter(q=>(S.adminCategory==='Todos'||q.category_name===S.adminCategory)&&(!S.adminSearch||q.question_text.toLowerCase().includes(S.adminSearch.toLowerCase())));
    const pendingCats=S.adminCategories.filter(c=>!c.approved);
    const pendingQs=S.adminQuestions.filter(q=>q.approval_status&&q.approval_status!=='approved');
    const ach=S.adminAchievements||[];
    const catOptions=cats.map(c=>`<option value="${esc(c.id)}">${esc(c.name)}</option>`).join('');
    const qCatOptions=cats.map(c=>`<option>${esc(c.name)}</option>`).join('');
    const packageHtml=(S.adminCategorySubmissions||[]).map(x=>{const qs=Array.isArray(x.questions)?x.questions:[];return `<div class="question-admin category-package-admin"><div><span class="badge">NOVO TÓPICO</span><span class="badge">${qs.length} PERGUNTAS</span><h3>${esc(x.name)}</h3><div class="muted">${esc(x.description||'Sem descrição')} • enviado por ${esc(x.creator_username||'Jogador')} • ${new Date(x.created_at).toLocaleString('pt-BR')}</div><details style="margin-top:10px"><summary>REVISAR PERGUNTAS</summary><div class="package-review">${qs.map((q,i)=>`<div class="package-review-q"><b>${i+1}. ${esc(q.question_text)}</b><div>${(q.options||[]).map((o,j)=>`<span class="badge ${j===Number(q.correct_index)?'ok':''}">${'ABCD'[j]}) ${esc(o)}${j===Number(q.correct_index)?' ✓':''}</span>`).join(' ')}</div></div>`).join('')}</div></details></div><div class="admin-actions"><button class="secondary approvePackage" data-id="${x.id}">✓ APROVAR TUDO</button><button class="danger rejectPackage" data-id="${x.id}">✕ REJEITAR</button></div></div>`}).join('')||'<div class="muted">Nenhum pacote de categoria aguardando aprovação.</div>';
    const pendingCatHtml=pendingCats.length?pendingCats.map(c=>`<div class="question-admin"><b>${esc(c.name)}</b><div class="muted">${c.parent_id?'Subcategoria':'Categoria'} • ${esc(c.description||'Sem descrição')}</div><div class="admin-actions"><button class="secondary approveCategory" data-id="${c.id}">✓ APROVAR</button><button class="danger rejectCategory" data-id="${c.id}">✕ EXCLUIR</button></div></div>`).join(''):'<div class="muted">Nenhuma categoria pendente.</div>';
    const pendingQHtml=pendingQs.length?pendingQs.map(q=>`<div class="question-admin"><span class="badge">${esc(q.category_name)}</span><span class="badge">PENDENTE</span><p><b>${esc(q.question_text)}</b></p><div class="admin-actions"><button class="secondary approveQuestion" data-id="${q.id}">✓ APROVAR</button><button class="danger rejectQuestion" data-id="${q.id}">✕ EXCLUIR</button></div></div>`).join(''):'<div class="muted">Nenhuma pergunta pendente.</div>';
    const questionHtml=filtered.map(q=>`<div class="question-admin ${q.active?'':'inactive'}"><div><span class="badge">${esc(q.category_name)}</span><span class="badge ${q.active?'ok':'off'}">${q.active?'ATIVA':'INATIVA'}</span>${q.approval_status&&q.approval_status!=='approved'?'<span class="badge">PENDENTE</span>':''}<p><b>${esc(q.question_text)}</b></p><div class="muted">${(q.options||[]).map((o,i)=>`${'ABCD'[i]}) ${esc(o)}${i===q.correct_index?' ✓':''}`).join(' • ')}</div></div><div class="admin-actions"><button class="secondary editQuestion" data-id="${q.id}">✏️ EDITAR</button><button class="secondary toggleQuestion" data-id="${q.id}" data-active="${q.active}">${q.active?'⏸ INATIVAR':'▶ ATIVAR'}</button><button class="danger deleteQuestion" data-id="${q.id}">🗑 EXCLUIR</button></div></div>`).join('');
    const achievementHtml=ach.map(a=>`<div class="question-admin ${a.active?'':'inactive'}"><div><b>${esc(a.icon||'🏆')} ${esc(a.title)}</b><div class="muted">${esc(a.description||'')}</div></div><div class="admin-actions"><button class="secondary toggleAchievement" data-id="${a.id}" data-active="${a.active}">${a.active?'⏸ INATIVAR':'▶ ATIVAR'}</button><button class="danger deleteAchievement" data-id="${a.id}">🗑 EXCLUIR</button></div></div>`).join('');
    return `<div class="title">Painel do Administrador</div><button class="secondary" id="adminHome">← VOLTAR PARA A PÁGINA INICIAL</button><div class="card pad" style="margin-top:12px"><b>Solicitações de novos tópicos completos</b><p class="muted">O jogador só consegue enviar quando preencher 10 perguntas válidas. Aprovar aqui publica a categoria e ativa as 10 perguntas de uma vez.</p>${packageHtml}</div><div class="card pad" style="margin-top:12px"><b>Nova categoria administrativa</b><form id="catForm" class="form"><input id="catName" placeholder="Nome da categoria" required><select id="catParent"><option value="">Categoria principal</option>${catOptions}</select><input id="catIcon" placeholder="Ícone, ex.: ⚽"><textarea id="catDesc" placeholder="Descrição"></textarea><button class="primary">CRIAR CATEGORIA</button></form></div><div class="card pad" style="margin-top:12px"><b>Nova pergunta administrativa</b><form id="qForm" class="form"><select id="qCat" required>${qCatOptions}</select><textarea id="qText" required placeholder="Digite a pergunta"></textarea><input id="q0" required placeholder="Alternativa A"><input id="q1" required placeholder="Alternativa B"><input id="q2" required placeholder="Alternativa C"><input id="q3" required placeholder="Alternativa D"><select id="qCorrect"><option value="0">A é correta</option><option value="1">B é correta</option><option value="2">C é correta</option><option value="3">D é correta</option></select><input id="qImage" type="url" placeholder="URL da imagem (opcional)"><button class="primary">SALVAR PERGUNTA</button></form></div><div class="card pad" style="margin-top:12px"><div class="admin-head"><b>Solicitações pendentes antigas</b></div><h4>Categorias aguardando aprovação</h4>${pendingCatHtml}<h4 style="margin-top:16px">Perguntas aguardando aprovação</h4>${pendingQHtml}</div><div class="card pad" style="margin-top:12px"><div class="admin-head"><b>Perguntas cadastradas (${filtered.length})</b><button class="secondary" id="reloadQuestions">ATUALIZAR</button></div><div class="admin-filters"><input id="adminSearch" value="${esc(S.adminSearch)}" placeholder="Pesquisar pergunta..."><select id="adminCategory">${qcats.map(c=>`<option ${c===S.adminCategory?'selected':''}>${esc(c)}</option>`).join('')}</select></div><div class="admin-list">${questionHtml||'<div class="muted">Nenhuma pergunta encontrada.</div>'}</div></div><div class="card pad" style="margin-top:12px"><b>Conquistas</b><form id="achievementForm" class="form" style="margin-top:10px"><input id="achTitle" required maxlength="80" placeholder="Nome da conquista"><textarea id="achDesc" maxlength="180" placeholder="Descrição / objetivo"></textarea><input id="achIcon" maxlength="4" placeholder="Ícone, ex.: 🏆"><button class="primary">CRIAR CONQUISTA</button></form><div class="admin-list" style="margin-top:12px">${achievementHtml||'<div class="muted">Nenhuma conquista criada.</div>'}</div></div>`;
  }
  async function loadCategories(){if(!sb)return;const {data,error}=await sb.from('categories').select('id,name,icon,description,parent_id,approved,created_by').eq('approved',true).order('name');if(!error&&data)S.categories=data;else if(!S.categories.length)S.categories=catFallback();}
  async function loadAchievements(){if(!sb)return;const {data,error}=await sb.from('achievements').select('id,title,description,icon,active').eq('active',true).order('created_at');if(error){console.error(error);S.achievementsRows=[]}else S.achievementsRows=data||[];render()}
  async function loadAdminQuestions(){if(!sb||!S.isAdmin)return;const {data,error}=await sb.from('questions').select('id,category_name,question_text,options,correct_index,image_url,active,approval_status,created_at,created_by').order('created_at',{ascending:false});if(error)alert(error.message);else{S.adminQuestions=data||[];await loadAdminCategories();await loadAdminCategorySubmissions();await loadAdminAchievements();render()}}
  async function loadAdminCategorySubmissions(){if(!sb||!S.isAdmin)return;const {data,error}=await sb.rpc('admin_list_category_submissions');if(!error)S.adminCategorySubmissions=data||[];else console.error(error)}
  async function approveCategoryPackage(id,ok){if(!sb||!id)return;if(!confirm(ok?'Aprovar esta categoria e publicar todas as perguntas?':'Rejeitar este pacote de categoria?'))return;const {data,error}=await sb.rpc('admin_review_category_submission',{p_submission_id:id,p_approve:ok});if(error)return alert(error.message);alert(ok?'Categoria publicada com sucesso!':'Solicitação rejeitada.');await loadAdminQuestions()}
  async function loadAdminCategories(){if(!sb||!S.isAdmin)return;const {data,error}=await sb.from('categories').select('id,name,icon,description,parent_id,approved,created_by,created_at').order('created_at',{ascending:false});if(!error)S.adminCategories=data||[]}
  async function loadAdminAchievements(){if(!sb||!S.isAdmin)return;const {data,error}=await sb.from('achievements').select('id,title,description,icon,active,created_at').order('created_at',{ascending:false});if(!error)S.adminAchievements=data||[]}
  async function editQuestion(id){const q=S.adminQuestions.find(x=>x.id===id);if(!q)return;const text=prompt('Pergunta:',q.question_text);if(text===null)return;const opts=(q.options||[]).map((o,i)=>prompt(`Alternativa ${'ABCD'[i]}:`,o));if(opts.some(o=>o===null))return;const correct=prompt('Índice da correta: 0=A, 1=B, 2=C, 3=D',String(q.correct_index));if(correct===null)return;const image=prompt('URL da imagem (opcional):',q.image_url||'');const {error}=await sb.from('questions').update({question_text:text.trim(),options:opts,correct_index:Number(correct),image_url:image.trim()||null}).eq('id',id);if(error)alert(error.message);else await loadAdminQuestions()}
  async function toggleQuestion(id,active){const {error}=await sb.from('questions').update({active:!active}).eq('id',id);if(error)alert(error.message);else await loadAdminQuestions()}
  async function deleteQuestion(id){if(!confirm('Excluir esta pergunta definitivamente?'))return;const {error}=await sb.from('questions').delete().eq('id',id);if(error)alert(error.message);else await loadAdminQuestions()}
  async function approveCategory(id,ok){if(ok){const {error}=await sb.from('categories').update({approved:true}).eq('id',id);if(error)return alert(error.message)}else{const {error}=await sb.from('categories').delete().eq('id',id);if(error)return alert(error.message)}await loadAdminQuestions()}
  async function approveQuestion(id,ok){if(ok){const {error}=await sb.from('questions').update({approval_status:'approved',active:true}).eq('id',id);if(error)return alert(error.message)}else{const {error}=await sb.from('questions').delete().eq('id',id);if(error)return alert(error.message)}await loadAdminQuestions()}
  async function createAchievement(){const title=$('#achTitle')?.value.trim();if(!title)return;const {error}=await sb.from('achievements').insert({title,description:$('#achDesc').value.trim()||null,icon:$('#achIcon').value.trim()||'🏆',active:true,created_by:S.user.id});if(error)alert(error.message);else{alert('Conquista criada.');$('#achievementForm').reset();await loadAdminQuestions()}}
  async function toggleAchievement(id,active){const {error}=await sb.from('achievements').update({active:!active}).eq('id',id);if(error)alert(error.message);else await loadAdminQuestions()}
  async function deleteAchievement(id){if(!confirm('Excluir esta conquista?'))return;const {error}=await sb.from('achievements').delete().eq('id',id);if(error)alert(error.message);else await loadAdminQuestions()}
  async function loadProfile(){if(!sb||!S.user)return;const {data}=await sb.from('profiles').select('*').eq('id',S.user.id).single();S.profile=data||{username:S.user.email?.split('@')[0],display_name:S.user.email?.split('@')[0]};S.isAdmin=S.profile.role==='admin'}
  async function sendFriend(username){if(!sb||!username)return;const {data,error}=await sb.rpc('send_friend_request',{p_username:username.trim()});if(error)alert(error.message);else{alert(data?.existing?'Esse jogador já possui uma relação de amizade/solicitação com você.':'Solicitação enviada.');S.friendRows=null;go('friends')}}
  async function acceptFriend(id){const {error}=await sb.from('friendships').update({status:'accepted'}).eq('id',id).eq('addressee_id',S.user.id);if(error)alert(error.message);else{S.friendRows=null;go('friends')}}
  function chat(){const name=S.chatTarget?.username||'Amigo';const rows=S.chatRows||[];return `<div class="title chat-title">💬 ${esc(name)}</div><div class="card chat-box"><div id="chatMessages" class="chat-messages">${rows.length?rows.map(m=>{const mine=m.sender_id===S.user.id;const photo=m.message_type==='photo'&&m.media_url;return `<div class="chat-msg ${mine?'mine':''}"><div class="bubble">${photo?`<img src="${esc(m.media_url)}" class="chat-photo" alt="Foto enviada">`:`<div>${esc(m.message)}</div>`}<small>${new Date(m.created_at).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}</small></div></div>`}).join(''):'<div class="muted center">Nenhuma mensagem ainda. Diga oi 👋</div>'}</div>${S.emojiOpen?`<div class="emoji-panel">${['😀','😂','😍','😎','🥳','👍','❤️','🔥','👏','😢','😡','🤔','🎉','💪','🙏','👋','🤣','😘','💯','⚡'].map(e=>`<button type="button" class="emoji-btn" data-emoji="${e}">${e}</button>`).join('')}</div>`:''}<form id="chatForm" class="chat-compose"><button type="button" class="chat-tool" id="emojiBtn">😊</button><label class="chat-tool" for="chatPhoto">📷</label><input id="chatPhoto" type="file" accept="image/*" hidden><input id="chatInput" maxlength="500" autocomplete="off" placeholder="Digite uma mensagem..."><button class="send-chat" type="submit">➤</button></form></div><button class="secondary" id="backFriends">← VOLTAR AOS AMIGOS</button>`}
  async function openChat(id,name){if(!sb||!id)return;S.chatTargetId=id;S.chatTarget={id,username:name};const {data,error}=await sb.from('direct_messages').select('id,sender_id,receiver_id,message,message_type,media_url,created_at').or(`and(sender_id.eq.${S.user.id},receiver_id.eq.${id}),and(sender_id.eq.${id},receiver_id.eq.${S.user.id})`).order('created_at',{ascending:true});if(error)return alert(error.message);S.chatRows=data||[];if(S.realtime)sb.removeChannel(S.realtime);S.realtime=sb.channel(`chat-${[S.user.id,id].sort().join('-')}`).on('postgres_changes',{event:'INSERT',schema:'public',table:'direct_messages'},payload=>{const m=payload.new;if((m.sender_id===S.user.id&&m.receiver_id===id)||(m.sender_id===id&&m.receiver_id===S.user.id)){if(!S.chatRows.some(x=>x.id===m.id)){S.chatRows.push(m);render();}}}).subscribe();go('chat')}
  async function sendChat(){const text=$('#chatInput')?.value.trim();if(!text||!S.chatTargetId)return;const {error}=await sb.from('direct_messages').insert({sender_id:S.user.id,receiver_id:S.chatTargetId,message:text,message_type:'text'});if(error)alert(error.message);else{$('#chatInput').value='';S.emojiOpen=false;await openChat(S.chatTargetId,S.chatTarget.username)}}
  async function sendChatPhoto(file){if(!file||!S.chatTargetId)return;if(file.size>5*1024*1024)return alert('A foto deve ter no máximo 5 MB.');const ext=(file.name.split('.').pop()||'jpg').toLowerCase();const path=`${S.user.id}/${Date.now()}-${Math.random().toString(36).slice(2)}.${ext}`;const {error:up}=await sb.storage.from('chat-media').upload(path,file,{cacheControl:'3600',upsert:false,contentType:file.type});if(up)return alert(up.message);const {data}=sb.storage.from('chat-media').getPublicUrl(path);const {error}=await sb.from('direct_messages').insert({sender_id:S.user.id,receiver_id:S.chatTargetId,message:'📷 Foto',message_type:'photo',media_url:data.publicUrl});if(error)alert(error.message);else await openChat(S.chatTargetId,S.chatTarget.username)}
  function challengeFriend(id,name){S.challengeTargetId=id;S.chatTarget=null;S.challengeSearch='';S.challengeModal=true;render()}
  async function chooseChallengeCategory(category){const id=S.challengeTargetId;S.challengeModal=false;render();const {data,error}=await sb.rpc('create_friend_challenge',{p_friend_id:id,p_category:category});if(error)return alert(error.message);alert('⚔️ Desafio enviado! Você já pode jogar agora; seu amigo poderá responder quando entrar.');await openAsyncChallenge(data)}
  async function acceptChallenge(id){await openAsyncChallenge(id)}
  async function bindAdmin(){if(!sb)return;$('#adminHome')?.addEventListener('click',()=>go('home'));$('#catForm')?.addEventListener('submit',async e=>{e.preventDefault();const {error}=await sb.from('categories').insert({name:$('#catName').value.trim(),icon:$('#catIcon').value.trim()||'🌐',description:$('#catDesc').value.trim()||null,parent_id:$('#catParent').value||null,approved:true,created_by:S.user.id});if(error)alert(error.message);else{alert('Categoria criada.');await loadAdminQuestions()}});$('#qForm')?.addEventListener('submit',async e=>{e.preventDefault();const options=[$('#q0').value.trim(),$('#q1').value.trim(),$('#q2').value.trim(),$('#q3').value.trim()];const {error}=await sb.from('questions').insert({category_name:$('#qCat').value,question_text:$('#qText').value.trim(),options,correct_index:+$('#qCorrect').value,image_url:$('#qImage').value.trim()||null,created_by:S.user.id,approval_status:'approved',active:true});if(error)alert(error.message);else{alert('Pergunta salva.');await loadAdminQuestions()}});$('#achievementForm')?.addEventListener('submit',e=>{e.preventDefault();createAchievement()});$('#reloadQuestions')?.addEventListener('click',loadAdminQuestions);$('#adminSearch')?.addEventListener('input',e=>{S.adminSearch=e.target.value;render()});$('#adminCategory')?.addEventListener('change',e=>{S.adminCategory=e.target.value;render()});$$('.editQuestion').forEach(b=>b.onclick=()=>editQuestion(b.dataset.id));$$('.toggleQuestion').forEach(b=>b.onclick=()=>toggleQuestion(b.dataset.id,b.dataset.active==='true'));$$('.deleteQuestion').forEach(b=>b.onclick=()=>deleteQuestion(b.dataset.id));$$('.approveCategory').forEach(b=>b.onclick=()=>approveCategory(b.dataset.id,true));$$('.rejectCategory').forEach(b=>b.onclick=()=>approveCategory(b.dataset.id,false));$$('.approveQuestion').forEach(b=>b.onclick=()=>approveQuestion(b.dataset.id,true));$$('.rejectQuestion').forEach(b=>b.onclick=()=>approveQuestion(b.dataset.id,false));$$('.approvePackage').forEach(b=>b.onclick=()=>approveCategoryPackage(b.dataset.id,true));$$('.rejectPackage').forEach(b=>b.onclick=()=>approveCategoryPackage(b.dataset.id,false));$$('.toggleAchievement').forEach(b=>b.onclick=()=>toggleAchievement(b.dataset.id,b.dataset.active==='true'));$$('.deleteAchievement').forEach(b=>b.onclick=()=>deleteAchievement(b.dataset.id))}
  function bind(){
    $('#soundToggle')?.addEventListener('click',()=>{S.soundOn=!S.soundOn;localStorage.setItem('quizup_sound',S.soundOn?'on':'off');if(S.soundOn)audio();render()});
    $$('.primary,.secondary,.quick button,.power button,.classic-bottom button,.cat,.challenge-cat,.chat-tool,.send-chat').forEach(b=>b.addEventListener('click',()=>{if(b.id!=='soundToggle')sound('click')},{once:true}));
    $$('.classic-bottom button').forEach(b=>b.onclick=()=>{if(b.dataset.r==='profile')goProfile();else if(b.dataset.r)go(b.dataset.r)});$('#classicHome')?.addEventListener('click',()=>go('home'));$('#topProfile')?.addEventListener('click',()=>goProfile());$('#quickPlay')?.addEventListener('click',()=>go('categories'));$('#topSearch')?.addEventListener('click',()=>go('categories'));$$('[data-go]')?.forEach(b=>b.addEventListener('click',()=>go(b.dataset.go)));$$('[data-topic]').forEach(b=>b.onclick=()=>{S.topicCategory=b.dataset.topic;S.topicStats=null;S.topicRows=null;go('topic')});
    $$('.quick button').forEach(b=>b.onclick=()=>go(b.dataset.go));
    $$('.player-row').forEach(b=>b.onclick=()=>goProfile(b.dataset.profile));
    $$('.plain-profile').forEach(b=>b.onclick=()=>b.dataset.profile&&goProfile(b.dataset.profile));
    $('#notificationsBtn')?.addEventListener('click',async()=>{await loadNotifications();go('notifications')});$('#notificationsHomeTop')?.addEventListener('click',()=>go('notifications'));$('#notificationsHome')?.addEventListener('click',()=>go('home'));$('#markAllNotifications')?.addEventListener('click',markAllNotifications);$$('.notification-item').forEach(el=>el.onclick=()=>{const n=S.notifications.find(x=>x.id===el.dataset.notificationId);openNotification(n)});    $('#signupGo')?.addEventListener('click',()=>go('signup'));$('#loginGo')?.addEventListener('click',()=>go('login'));
    $('#loginForm')?.addEventListener('submit',async e=>{e.preventDefault();
      if(!sb)return alert('Supabase não está configurado. Configure o config.js antes de entrar.');
      let identifier=$('#identifier').value.trim();let email=identifier;if(!identifier.includes('@')){const {data,error}=await sb.rpc('get_login_email',{p_username:identifier});if(error||!data)return alert('Nome de usuário não encontrado.');email=data}const {error}=await sb.auth.signInWithPassword({email,password:$('#password').value});if(error){S.enterMultiplayer=false;alert(error.message)}
    });
    $('#signupForm')?.addEventListener('submit',async e=>{e.preventDefault();if(!sb)return;const username=$('#username').value.trim();if(!/^[A-Za-z0-9_]{3,20}$/.test(username))return alert('Nome de usuário inválido. Use 3 a 20 caracteres: letras, números e _.');const {data,error}=await sb.auth.signUp({email:$('#email').value.trim(),password:$('#password').value,options:{data:{username:username.toLowerCase(),display_name:username}}});if(error){if(/already|duplicate|uso|unique/i.test(error.message))alert('Esse nome de usuário já está em uso.');else alert(error.message);return}alert(data?.session?'Conta criada com sucesso!':'Conta criada. Verifique o e-mail se a confirmação estiver ativada no Supabase.');if(data?.session)go('home');else go('login')});
    $('#logout')?.addEventListener('click',async()=>{if(S.challengeRealtime&&sb){sb.removeChannel(S.challengeRealtime);S.challengeRealtime=null}if(S.notificationRealtime&&sb){sb.removeChannel(S.notificationRealtime);S.notificationRealtime=null}if(sb)await sb.auth.signOut();S.user=null;S.profile=null;go('login')});
    $('#play')?.addEventListener('click',()=>{S.mode='1v1';go('categories')});
    $$('.cat,.topic-list-row').forEach(b=>b.onclick=()=>{S.topicCategory=b.dataset.cat;S.topicStats=null;S.topicRows=null;go('topic')});$('#topicPlay')?.addEventListener('click',()=>{S.category=S.topicCategory;S.mode='1v1';S.searching=false;S.searchStartedAt=Date.now();S.match=null;S.matchId=null;S.botOffer=false;go('match')});$('#topicRanking')?.addEventListener('click',()=>{S.rankCategory=S.topicCategory;S.rankRows=null;go('ranking')});$('#backTopics')?.addEventListener('click',()=>go('categories'));
    $('#cancel')?.addEventListener('click',async()=>{if(sb)await sb.rpc('cancel_match_queue');S.searching=false;S.botOffer=false;go('categories')});$('#playBot')?.addEventListener('click',startBotMatch);$('#continueReal')?.addEventListener('click',()=>{S.botOffer=false;S.searchStartedAt=Date.now();S.searching=true;findMatch()});
    $$('.answer').forEach(b=>b.onclick=()=>answer(+b.dataset.a));$$('[data-async-a]').forEach(b=>b.onclick=()=>submitAsyncAnswer(+b.dataset.asyncA));$('#asyncBackFriends')?.addEventListener('click',()=>{S.asyncMode=false;S.asyncChallengeId=null;go('friends')});
    $('#plus')?.addEventListener('click',()=>{if(!S.plusUsed&&!S.answered&&!S.waiting){S.plusUsed=true;/* bônus visual apenas; o relógio oficial continua em 10s no modo clássico */}});
    $('#fifty')?.addEventListener('click',()=>{if(S.fiftyUsed||S.answered)return;S.fiftyUsed=true;const q=S.questions[S.questionIndex];if(!q)return;const correct=q.correct_index;[0,1,2,3].filter(i=>i!==correct).sort(()=>Math.random()-.5).slice(0,2).forEach(i=>document.querySelector(`[data-a="${i}"]`)?.classList.add('dim'))});
    $('#rematch')?.addEventListener('click',requestRematch);$('#findAnother')?.addEventListener('click',()=>{S.rematchMessage='';S.category=S.category;S.searching=false;S.searchStartedAt=Date.now();go('match')});$('#again')?.addEventListener('click',()=>go('categories'));$('#home')?.addEventListener('click',()=>go('home'));$('#friendsBtn')?.addEventListener('click',()=>go('friends'));$('#backRank')?.addEventListener('click',()=>go('ranking'));$('#adminBtn')?.addEventListener('click',()=>go('admin'));$('#rankCategory')?.addEventListener('change',e=>{S.rankCategory=e.target.value;S.rankRows=null;loadRanking()});$('#resultChat')?.addEventListener('click',()=>{const id=(S.match?.player_ids||[]).find(x=>x!==S.user.id);if(id)openChat(id,S.matchPlayers[id]?.username||'Oponente')});$('#publishResult')?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(`QuizUp • ${S.category} • ${S.myScore||getScore(S.user.id)} pontos`);alert('Resultado copiado para compartilhar!')}catch(e){alert('Resultado pronto para compartilhar!')}});
    $('#friendSearch')?.addEventListener('submit',e=>{e.preventDefault();sendFriend($('#friendName').value)});$('#closeChallenge')?.addEventListener('click',()=>{S.challengeModal=false;render()});$('#challengeSearch')?.addEventListener('input',e=>{S.challengeSearch=e.target.value;render();setTimeout(()=>$('#challengeSearch')?.focus(),0)});$$('[data-challenge-cat]').forEach(b=>b.onclick=()=>chooseChallengeCategory(b.dataset.challengeCat));$$('.acceptFriend').forEach(b=>b.onclick=()=>acceptFriend(b.dataset.id));$$('.chatFriend').forEach(b=>b.onclick=()=>openChat(b.dataset.id,b.dataset.name));$$('.challengeFriend').forEach(b=>b.onclick=()=>challengeFriend(b.dataset.id,b.dataset.name));$$('.acceptChallenge').forEach(b=>b.onclick=()=>acceptChallenge(b.dataset.id));$$('.continueChallenge').forEach(b=>b.onclick=()=>openAsyncChallenge(b.dataset.id));$('#friendsHome')?.addEventListener('click',()=>go('home'));$('#backFriends')?.addEventListener('click',()=>{if(S.realtime){sb.removeChannel(S.realtime);S.realtime=null}S.friendRows=null;go('friends')});$('#chatForm')?.addEventListener('submit',e=>{e.preventDefault();sendChat()});$('#emojiBtn')?.addEventListener('click',()=>{S.emojiOpen=!S.emojiOpen;render()});$$('.emoji-btn').forEach(b=>b.onclick=()=>{const input=$('#chatInput');if(input){input.value+=(b.dataset.emoji||'');input.focus()}S.emojiOpen=false;render();setTimeout(()=>$('#chatInput')?.focus(),0)});$('#chatPhoto')?.addEventListener('change',e=>sendChatPhoto(e.target.files?.[0]));
    $('#avatarFile')?.addEventListener('change',e=>uploadAvatar(e.target.files?.[0]));
    $('#categorySearch')?.addEventListener('input',e=>{S.categorySearch=e.target.value;render()});$('#clearCategorySearch')?.addEventListener('click',()=>{S.categorySearch='';render()});$('#contributeGo')?.addEventListener('click',()=>go('contribute'));$('#backCategories')?.addEventListener('click',()=>go('categories'));
    $('#categoryPackageForm')?.addEventListener('submit',async e=>{e.preventDefault();if(!sb||!S.user)return;const name=$('#packageCatName').value.trim();const icon=$('#packageCatIcon').value.trim()||'🌐';const description=$('#packageCatDesc').value.trim()||null;if(name.length<2)return alert('Digite um nome válido para a categoria.');const questions=[];for(let i=0;i<10;i++){const text=$(`#cqText${i}`).value.trim();const options=[0,1,2,3].map(j=>$(`#cq${j}_${i}`).value.trim());const correct=Number($(`#cqCorrect${i}`).value);if(!text||options.some(x=>!x))return alert(`Preencha a pergunta ${i+1} e as 4 respostas.`);if(new Set(options.map(x=>x.toLowerCase())).size<4)return alert(`A pergunta ${i+1} precisa ter 4 respostas diferentes.`);if(!Number.isInteger(correct)||correct<0||correct>3)return alert(`Escolha a resposta correta da pergunta ${i+1}.`);questions.push({question_text:text,options,correct_index:correct});}const btn=$('#submitCategoryPackage');if(btn)btn.disabled=true;const {data,error}=await sb.rpc('submit_category_package',{p_name:name,p_icon:icon,p_description:description,p_questions:questions});if(btn)btn.disabled=false;if(error)return alert(error.message);alert('✅ Categoria enviada! O administrador recebeu as 10 perguntas para revisão. Ela só aparecerá para outros jogadores depois da aprovação.');go('categories')});
    if(S.route==='admin'){bindAdmin();if(!S.adminQuestions.length)setTimeout(loadAdminQuestions,0)}
  }
  async function init(){if(!sb){S.user=null;S.profile=null;S.profileTargetId=null;return go('login')}const {data:{session}}=await sb.auth.getSession();S.accessToken=session?.access_token||null;const {data:{user}}=await sb.auth.getUser();S.user=user||null;if(!S.user)return go('login');subscribeChallenges();subscribeNotifications();await loadProfile();await loadCategories();await loadNotifications();S.achievementsRows=null;S.profileTargetId=S.user.id;go('home')}
  if(sb){sb.auth.getSession().then(init);sb.auth.onAuthStateChange((_event,session)=>{S.accessToken=session?.access_token||null;setTimeout(init,0)})}else init();
})();
